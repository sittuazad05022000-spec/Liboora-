# Liboora — Documentation Baseline Declaration

| Field | Value |
|---|---|
| **Baseline** | **BASELINE-2026-08-20-B** |
| **Supersedes** | `BASELINE-2026-08-20-A` |
| **Status** | **Locked** |
| **Date** | **2026-08-20** |
| **Declared by** | Enterprise Architecture review (`DOCUMENTATION_AUDIT-001`); re-issued by the **Governance Closure Phase**; extended by the **`PRD-004` Stage 7 freeze**, then by the **`PRD-005` Stage 7 freeze**, then by the **`PRD-007` Stage 7 freeze**, then by the **`PRD-006` Stage 7 freeze**, then by the **`PRD-008` Stage 7 freeze**, then by the **`PRD-013` Stage 7 freeze**, then by the **`PRD-016` Stage 7 freeze**, then by the **`PRD-014` Stage 7 freeze**, then by the **`PRD-023` Stage 7 freeze**, then by the **`PRD-017` Stage 7 freeze** |
| **Authority** | `ADR-0008`, `ADR-0009`, `ADR-0010`, `ADR-0011`, `ADR-0013`, `ADR-0014`, `ADR-0015`, `ADR-0016`, `ADR-0017`, `ADR-0018`, `ADR-0019`, `ADR-0020`, **`ADR-0033`**, **`ADR-0034`**, **`ADR-0049`**, **`ADR-0050`**, **`ADR-0051`**, **`ADR-0053`**, **`ADR-0054`** |

---

## 1. What this document does

This document declares the **official documentation baseline** for the Liboora platform. It fixes, at one point in
time, which documents are authoritative, at which versions, and in what order of precedence. Every other document
in the repository is either **in the baseline**, **superseded by it**, or **archived**.

Its purpose is to remove the question *"which document do I believe?"* from the developer's day.

---

## 2. Baseline declaration

**Authentication PRD v2.0 is the official baseline specification for `BC-18` Identity & Access.**

Defect `D-7` — *"the Authentication PRD is not present in this repository"* — is **CLOSED**.

It is closed **by authorship, not by recovery.** The original v1 text was sought and is not in the repository, not
in git history, and not recoverable from any versioned artefact. v2.0 was written from scratch against the binding
constraints that *were* present. It does not reproduce v1 and makes no claim about what v1 contained. Full
reasoning is recorded in [`ADR-0008`](./adr/ADR-0008-authentication-prd-v2-baseline.md).

**If the original v1 ever resurfaces it has no standing.** Reinstating any part of it would require a new ADR.

### 2.1 Second declaration — the Library PRD

**Library PRD v1.1 is the official baseline specification for the Library Management domain**, together with its
two normative extensions §14A and §14B and the Invitation Security Specification.

> `v1.1` amends **ownership statements and context pointers only** — `ADR-0013` (capability-context ownership) and
> `ADR-0015` (finance contexts `BC-05`/`BC-20`). **No requirement was added, removed or changed:** the identifier
> sets are provably identical to `v1.0` (129 `LIB-*`, 10 `LCFG-*`, 10 `LXC-*`, 22 `LEV-*`, 12 `LAC-*`). See
> `ADR-0013` §8.1.

Gap **`U-4`** — *"Library PRD §§1–25 never supplied"* — is **CLOSED by receipt.**

Unlike `D-7`, this is not closed by authorship. The sections were **supplied by the product owner** on 2026-08-03
and are preserved in `Library_PRD_v1.md`. Every requirement in the supplied text survives in the baseline. Fourteen
conflicts against higher-ranked documents were found and corrected; three of those were blocking. The corrections
are recorded, with the reasoning for each, in
[`LIBRARY_PRD_ALIGNMENT.md`](../30-product/library/LIBRARY_PRD_ALIGNMENT.md) §2. **Nothing was removed.**

Deferral **`AR-4` invitation security specification** is also **CLOSED**, by authorship, in
[`INVITATION_SECURITY_SPECIFICATION.md`](../30-product/library/INVITATION_SECURITY_SPECIFICATION.md) and
[`ADR-0009`](./adr/ADR-0009-invitation-security-model.md). The deferral instructed *"do not invent"*; its stated
precondition — that §§1–25 had not been received — no longer holds, and the received text confirms the invitation
feature exists in three distinct forms. It is lifted **on evidence, not by assumption.**

### 2.2 Third declaration — the Student Identity & Profile PRD

**Student Identity & Profile PRD v1.0 is the official baseline specification for `BC-10` Global Person
Identity**, together with its alignment report.

Like the Library PRD, this is closed **by receipt**: Chapters 1–5 were supplied by the product owner on
2026-08-04. Thirteen conflicts against higher-ranked documents were found; four were blocking. Eleven were
accepted and **two were rejected**, with the draft's original intent preserved in both cases. Every acceptance
and both rejections are recorded in
[`STUDENT_IDENTITY_ALIGNMENT.md`](../30-product/student-identity/STUDENT_IDENTITY_ALIGNMENT.md).
**No functionality was removed.**

**This declaration carries a structural change that the previous two did not.** Resolving conflict `SC-1`
required amending the Bounded Context Map's Identity Triad — the section the map itself flags as *"the
highest-risk modelling decision in LIBOORA"* — and consequentially amending global business rule
`MP-GBR-02`. That reasoning is recorded in
[`ADR-0011`](./adr/ADR-0011-global-person-identity.md), and the amendment is deliberately narrow: **placement,
name, classification and cardinality only.** Linkage rules `ID-1`…`ID-6` and prohibition `X-05` are preserved
unamended, and no dependency law gained an exception — rank 7.5 was introduced precisely so that none was
needed.

Open question **`Q-05`** — *"Is Global Student available to a person with no library enrollment?"* — is
**CLOSED: yes, necessarily.**

### 2.3 Fourth declaration — the Student Management PRD

**Student Management PRD v1.2 is the official baseline specification for `BC-01` Enrollment**, together with the
Library Members Directory read composition delivered as its chapter 5.

Unlike the previous three, this is closed **by authorship followed by three independent reviews.** It was drafted
on 2026-08-04, reviewed, corrected, reviewed a second time, corrected again, and then re-audited adversarially
against twelve mandated verification items — an audit that attempted **ten disproofs of the specification and
failed on eight outright.** Every finding, every correction and every **rejected** finding is recorded in the six
satellite reports listed in §3.3. Reasoning is in
[`ADR-0018`](./adr/ADR-0018-student-management-prd-v1.2-baseline.md).

**This declaration is the narrowest of the four. No ranked document is amended.** No bounded context was created —
the count remains **31**. No integration edge was added. No global business rule changed. No dependency law gained
an exception. `MASTER_PRD.md`, the Bounded Context Map, the Module Dependency Matrix and `ARCHITECTURE_RULINGS.md`
are **untouched**. Master PRD §8 **module 3** (Library Member Directory) is delivered as `PRD-004` chapter 5 —
`LMD-1`…`LMD-31`, owning no aggregate and no invariant per `AR-1`, so **no `BC-32` exists** and no second member
store was created.

**The version is preserved at v1.2.** Freeze confers status; it does not renumber. The Student Identity PRD was
admitted at v1.0 and remained v1.0; this document is admitted at the version that was verified.

> **Two gaps are admitted OPEN, and that is deliberate.** **`SM-GAP-10`** (is Date of Birth mandatory at `BC-10`?)
> and **`SM-GAP-11`** (is `BC-26` a consumer of `SM-EV-1`/`SM-EV-5`?) are tensions **internal to other frozen
> documents** — `PRD-003` §2.2 vs `SID-5.9`, and BC Map §8 vs §7 respectively. `PRD-004` **discloses** both and
> resolves neither, and its own behaviour is deterministic under every resolution. **Admission does not ratify
> either defect.** Each requires a separate ADR from its named owner — see `ADR-0018` §3.1 and §3.2. Recorded in
> §6 as `GCP-05` and `GCP-06`.

**`PRD-004` is `FROZEN`, not `VERIFIED`.** Implementation is **entirely unstarted** — 0 of 24 tasks
(`IMPL-300`…`IMPL-323`), and **0 of its 32 acceptance criteria are proven by a test.** Per `SID-4.56` a rule that
cannot be checked is treated as unmet, so nothing in this declaration should be read as a claim that `BC-01`
works.

---

## 3. Baseline contents

### 3.1 Governance

| Document | Version | Status |
|---|---|---|
| `00-governance/DOCUMENTATION_BASELINE.md` | **BASELINE-2026-08-20-B** | **This document.** *Advanced 2026-08-20 by the `PRD-017` File & Media admission (`ADR-0054`) — the **second** suffix advance within a single day, after `-A`.* ⚠ **This cell was CURRENT and correct when this admission began**, reading `BASELINE-2026-08-20-A` against a header row reading the same. That is worth recording positively: `GCP-15` was logged as RECURRING one admission ago, the `PRD-023` pass repaired it under §7 rule 3, **and the repair held through a second admission**. It is advanced here in the same edit as the header row, not afterwards. *Prior text retained verbatim:* *Advanced 2026-08-20 by the `PRD-023` admission (`ADR-0053`).* ⚠ **This cell read `BASELINE-2026-08-05-A` while the header row read `BASELINE-2026-08-19-C` — THREE baselines stale — and that is `GCP-15` RECURRING after `GCP-15` was recorded CLOSED on 2026-08-19.** It is repaired here because §7 rule 3 obliges this declaration to match the repository, and it is logged in `ADR-0053` §8.1 as **drift 3, a recurrence rather than a first occurrence**: the `-B` and `-C` admissions each moved the header without moving this cell, which is the identical failure `GCP-15` was opened for. **The eleventh instance of the derived-statement class.** *Prior text retained verbatim:* *This cell read `BASELINE-2026-08-04-D` while the header row read `BASELINE-2026-08-04-E` — a pre-existing drift of one issue, found and corrected here and logged as `GCP-11`. It is the third instance of the §7 step 3 defect class already recorded as `GCP-01` and `GCP-07`: a derived statement left behind by a change to the thing it describes* |
| `00-governance/adr/ADR-INDEX.md` | Active | Authoritative |
| `00-governance/adr/ADR-0001` … `ADR-0020`, `ADR-0032`, `ADR-0033`, `ADR-0034` | Accepted | Authoritative, binding. **23 of the 34 ADR files are `Accepted`; `ADR-0021`…`ADR-0031` are `Proposed` and bind nothing** — `ADR-0031` in particular remains `Proposed` because no **Product Owner** has approved a range for the presence tolerance |
| `00-governance/DOCUMENTATION_AUDIT-001.md` | 001 | Historical record of this review |

### 3.2 Architecture

| Document | Version | Status |
|---|---|---|
| `10-architecture/LIBOORA_BOUNDED_CONTEXT_MAP.md` | **v1.7** | Authoritative — boundaries, ownership, edges. Identity Triad §4 amended by `ADR-0011`; edge `E-22` consumer list amended by `ADR-0016`; edge **`E-27` added by `ADR-0032`** (v1.6) and **withdrawn again by `ADR-0033`** (v1.7), so **the edge set is once more `E-01`…`E-26`** — verified mechanically at **26 edge rows, identical to the pre-`ADR-0032` count**, with the neighbouring `E-08` row byte-identical. Context count **31, unchanged**. *This cell read **v1.5** through the v1.6 bump as well; the omission is a pre-existing §7 step 3 drift, corrected here and logged as `GCP-11`* |
| `10-architecture/LIBOORA_MODULE_DEPENDENCY_MATRIX.md` | **v1.3** | Authoritative — permitted and forbidden dependencies. Rank `R7.5` added by `ADR-0011`; enforcement coverage **12 of 12** per `ADR-0014` |
| `10-architecture/ARCHITECTURE_RULINGS.md` | **v1.2** | Authoritative for `AR-1`, `AR-3`, `AR-4`; `AR-2`, `AR-5`, `AR-6`, `AR-7` promoted to ADRs. `AR-4` deferral lifted |
| `10-architecture/LIBOORA_ENTERPRISE_ARCHITECTURE.md` | **v2.2** | **Descriptive** — must follow the PRDs, never lead them. *(Advanced **v2.1 → v2.2** on 2026-08-15 under `ACCEPTED` `ADR-0042` §4, per §7 step 3 — "this declaration is updated in the same commit". The EA changed by exactly **one token** — **L823** `Refunds (V2)` → `Refunds (V1)` — because Rank 4 `LIBOORA_BOUNDED_CONTEXT_MAP.md` **L100** places refunds in `BC-05` at Release **V1**, **L374** makes `RefundRecord` a declared `FeeLedger` member and **L422** publishes `fee.RefundIssued`. This is the **Descriptive** rule in this very cell operating as designed: the lower-ranked document was updated to match the higher-ranked ones, never the reverse (`MP-CON-08`, `MASTER_PRD.md` **L519**). ⚠ **The baseline identifier is deliberately NOT changed** — §7 step 4 permits it only when a **Rank 1–3** document changes version, and the EA is **Rank 6**. ⚠ **A residual drift is disclosed rather than repaired:** `MP-CON-08` cites the EA by the literal string "v2.1" inside **Rank 1** `MASTER_PRD.md` (7 occurrences), which this bump makes stale. Editing Rank 1 as a side effect of a Rank 6 version bump is the silent amendment §7 step 1 and `PRD_LIFECYCLE.md` **L177** forbid, so it is **routed to the Architecture Owner as `Q-A6`** in `ADR-0042` §11 and left visible here.)* |

### 3.3 Product

| Document | Version | Status |
|---|---|---|
| `30-product/MASTER_PRD.md` | **v1.7** | Authoritative — platform-wide global rules. `MP-GBR-02` amended by `ADR-0011` |
| `30-product/authentication/Authentication_PRD_v2.md` | **v2.0** | **Authoritative — the baseline** |
| `30-product/authentication/prd-v2/00` … `11` | v2.0 | Chapter sources of the above |
| `30-product/authentication/PRD-V2-GOVERNANCE-NOTE.md` | v2.0 | Provenance record — **not part of the specification** |
| `30-product/library/Library_PRD_v1.md` | **v1.1** | **Authoritative — the Library baseline**, §§1–25. Ownership header amended by `ADR-0013`; three finance context identifiers corrected by `ADR-0015`. **No requirement identifier changed** |
| `30-product/library/14A-Library-Discovery-And-Enrollment.md` | v1.0 | Authoritative — discovery, enrollment, public field list |
| `30-product/library/14B-Public-Library-Preview.md` | **v1.0** | Authoritative — anonymous preview, the authentication boundary `PO-1`…`PO-12` |
| `30-product/library/INVITATION_SECURITY_SPECIFICATION.md` | **v1.0** | Authoritative — `IT-1`…`IT-3`, entropy, expiry, revocation, single use, validation, audit, rate limiting |
| `30-product/library/LIBRARY_PRD_ALIGNMENT.md` | v1.0 | Validation record — **not part of the specification** |
| `30-product/library/REVIEW_14A.md` | v1.1 | Historical review record — **not part of the specification** |
| `30-product/student-identity/Student_Identity_PRD_v1.md` | **v1.0** | **Authoritative — the Student Identity baseline**, Chapters 1–5 |
| `30-product/student-identity/README.md` | v1.0 | Module index — navigational, **not part of the specification** |
| `30-product/student-identity/STUDENT_IDENTITY_ALIGNMENT.md` | v1.0 | Validation record, `SC-1`…`SC-13` — **not part of the specification** |
| `30-product/student-management/Student_Management_PRD_v1.md` | **v1.2** | **Authoritative — the Student Management baseline** for `BC-01` Enrollment, 10 chapters. **Chapter 5 is the Library Members Directory** (`LMD-1`…`LMD-31`) as an application-layer **read composition** — no aggregate, no invariant, no second member store (`AR-1`). 251 base identifiers (266 with sub-lettered), ten registers, ten `SM-EV-*` events closed by `SM-7.12`. Admitted by `ADR-0018`; **no requirement identifier changed on admission** |
| `30-product/student-management/README.md` | v1.0 | Module index — navigational, **not part of the specification** |
| `30-product/student-management/PRD-004_ALIGNMENT_REPORT.md` | v1.1 | Validation record, `CONF-1`…`CONF-3`, `DUP-1`…`DUP-4`, `SM-GAP-1`…`SM-GAP-11` — **not part of the specification** |
| `30-product/membership-management/PRD-MEMBERSHIP-MANAGEMENT.md` | **v1.4** | **Authoritative — the Membership Management baseline** for `BC-02` Membership, 26 chapters. Four authoritative capabilities (Plans, Creation, Renewal, Upgrade) plus Activation/Validity, Status/Entitlement, Expiry and Operational Views. **333 identifiers** across ten registers, all contiguous, zero duplicates, zero collisions; 320 obligation-bearing, 223 criterion-verifiable, 97 `MM-AC-*`; **seven `MM-EVT-*` closed at seven by `MM-BR-035`**. Verification coverage is **measured, not asserted** — 77/81 = 95.1% of the rule registers, 134/223 = 60.1% of all obligations, with the four unverified business rules named in `ADR-0019` §3.5. Admitted by `ADR-0019`; **no requirement identifier changed on admission**, and **no integration edge was added** |
| `30-product/membership-management/PRD-005_FIFTH_INDEPENDENT_REVIEW.md` | v1.0 | Validation record — the admission review, **verdict A — PASS**, zero findings, one non-blocking observation (`OBS-1`). **Not part of the specification** |
| `30-product/membership-management/PRD-005_BLOCKER_ANALYSIS.md`, `PRD-005_CORRECTION_REPORT.md`, `PRD-005_SECOND_INDEPENDENT_REVIEW.md`, `PRD-005_F1-F4_CORRECTION_REPORT.md`, `PRD-005_THIRD_INDEPENDENT_REVIEW.md`, `PRD-005_TR-1-TR-5_CORRECTION_REPORT.md`, `PRD-005_FOURTH_INDEPENDENT_REVIEW.md`, `PRD-005_QR-1-QR-2_CORRECTION_REPORT.md` | v1.0 | Review and correction history — five independent reviews, four correction passes, **two retracted claims preserved as annotations**. Historical records, **not part of the specification** |
| `30-product/seat-management/PRD-SEAT-MANAGEMENT.md` | **v1.0** | **Authoritative — the Seat Management baseline** for `BC-04` Seating, 36 chapters. Owns exactly **two aggregates**, `SeatAllocation` and `SeatLayout` (`SEAT-FR-006`), one aggregate per transaction (`SEAT-FR-007`). The **G1…G16 acquisition gate** (§8) is executed by all nine acquisition paths, stopping at first failure, with `G7` membership validity overridable by **no role including Owner** (`SEAT-BR-014`, `SEAT-XC-004`) and `G16` under a pessimistic lock held to commit (`SEAT-BR-015`). **683 identifiers** across ten registers, all contiguous, zero duplicates, zero collisions; 669 obligation-bearing, 443 criterion-verifiable, 226 `SEAT-AC-*`; **four `SEAT-EVT-*` closed at four by `SEAT-FR-206`**. Verification coverage is **measured, not asserted** — 85/85 = 100.0% of the four registers `SEAT-FR-302` mandates, 325/443 = 73.4% of all obligations, with the 118-obligation shortfall named in `ADR-0020` §2.5. Admitted by `ADR-0020`; **no requirement identifier changed on admission**, and **no integration edge was added** |
| `30-product/seat-management/README.md` | v1.0 | Module index — navigational, **not part of the specification** |
| `30-product/seat-management/PRD-007_STAGE4_FINAL_REVIEW.md` | v1.0 | Validation record — the Stage 4 requirements review, **verdict A — PASS**, 0 CRITICAL · 0 HIGH · 0 MEDIUM · 0 LOW, eight candidate findings **rejected with evidence**. **Not part of the specification** |
| `30-product/seat-management/PRD-007_STAGE5_TRACEABILITY.md`, `PRD-007_STAGE6_IMPLEMENTATION_TASKS.md`, `PRD-007_STAGE7_FREEZE.md` | v1.0 | Stage 5, 6 and 7 gate records — traceability, implementation-task allocation, freeze. Each **verdict A — PASS**. Validation records, **not part of the specification** |
| `30-product/seat-management/PRD-007_ARCHITECTURE_ALIGNMENT.md`, `PRD-007_REQUIREMENTS_REVIEW.md`, `PRD-007_INDEPENDENT_GAP_VERIFICATION.md`, `PRD-007_CORRECTION_RECORD.md`, `PRD-007_SR-1_CORRECTION_RECORD.md`, `PRD-007_STAGE4_REREVIEW.md`, `PRD-007_STAGE4_REREVIEW_2.md`, `PRD-007_STAGE4_FINDINGS_CORRECTION.md`, `PRD-007_STAGE4_COMPREHENSIVE_CORRECTION.md` | v1.0 | Review and correction history — architecture alignment, the requirements review, two re-reviews, four correction passes and an independent gap verification. Historical records, **not part of the specification** |
| `30-product/attendance-management/PRD-006_ATTENDANCE-MANAGEMENT.md` | **v1.9** | **Authoritative — the Attendance Management baseline** for `BC-03` Attendance, 33 chapters. Owns exactly **one aggregate**, `AttendanceDay` — *one student-day, **not** one punch*. Presence is established by qualifying Wi-Fi association with a **5-minute grace** and an Owner/Manager-configurable **30-minute tolerance**; the **final** qualifying disconnect is the attendance exit, and **no exit is ever inferred from absence of signal or from process death**. Multi-device presence is an **OR** over devices; **Location Verification is optional and defaults OFF**. Manual attendance is recorded but is **not verified presence**, and **seat allocation is not presence**. The Seat Card's presence renderings are **composed at read time** and it **never reads raw Wi-Fi or device state**. **Correction, recorded rather than quietly amended:** this cell previously read *“The Seat Card has **exactly four states** — NOT ATTENDED, PRESENT, ATTENDED·PRESENCE UNVERIFIED, ATTENDED·LEFT”*. **That enumeration is withdrawn as unsourced.** Measured against the documents themselves: the frozen PRD names those four labels **zero times** — its §23.4a table lists **six** requested renderings (`PRESENT`, `PRESENCE CHECKING`, `NOT PRESENT`, `OUTSIDE SHIFT WINDOW`, `SHIFT OVERSTAY / PRESENCE OUTSIDE BOOKED WINDOW`, `NO BOOKED SHIFT / PRESENCE UNASSIGNED`), and the four-label vocabulary belongs to [`ADR-0032`](adr/ADR-0032-attendance-presence-visibility-to-seating.md) **L302**, whose very next row **L304 records `ATTENDED · LEFT` as still BLOCKED** — `SEAT-BR-036` puts period aggregation in `BC-26` and `SEAT-FR-114` resets occupancy daily, so `BC-04` cannot answer *“attended earlier today”* after departure. The derived statement had therefore **promoted a blocked rendering to a delivered one** and fixed a count the source does not fix. **What is actually load-bearing survives unchanged and is what this baseline admits:** read-time composition, no raw Wi-Fi or device state on the card, manual attendance is not verified presence, and seat allocation is not presence. Logged as `GCP-12` in §6. **Exactly four domain events**, closed at four by the PRD's own business rule: no fifth event was created and `StudentCheckedOut` was **not** overloaded. **525 identifiers** across nine registers, all contiguous, zero duplicates, zero collisions; **285 obligation-bearing**, **222 acceptance criteria**. Verification coverage is **measured, not asserted** — **285/285 = 100.0%**. Admitted by `ADR-0034`; **no requirement identifier changed on admission**, and **the net integration-edge count is unchanged** (`E-27` was added by `ADR-0032` and withdrawn by `ADR-0033`). **18 gap-ledger entries remain OPEN with named owners** |
| `30-product/attendance-management/PRD-006_STAGE4_REREVIEW_V19.md` | v1.1 | Validation record — the Stage 4 requirements re-review of **v1.9**, **verdict A — PASS**, 6 of 6 checks. **Not part of the specification** |
| `30-product/attendance-management/PRD-006_ARCHITECTURE_ALIGNMENT_V19_SUPPLEMENT.md` | **v1.1** | Validation record — the Stage 3 architecture alignment **re-run after the dependency decision**, **verdict ✅ ALIGNED, 6 of 6 lifecycle checks PASS**. Its superseded **v1.0 ⛔ NOT ALIGNED** verdict is **retained inside the document beside the new one** rather than overwritten, so the record shows what was found as well as what was concluded. **Not part of the specification** |
| `30-product/attendance-management/PRD-006_E27_CLUSTER_EDGE_CONFLICT.md` | v1.1 | Validation record — the Rank 4 intra-cluster allow-list conflict, **RESOLVED by `ADR-0033` option `O-C`** (withdraw the edge; do **not** widen the allow-list). **Not part of the specification** |
| `30-product/attendance-management/PRD-006_ARCHITECTURE_ALIGNMENT.md`, `PRD-006_REQUIREMENTS_REVIEW.md`, `PRD-006_STAGE4_RE_REVIEW.md`, `PRD-006_STAGE4_FINDINGS_CORRECTION.md`, `PRD-006_PO_DECISION_RESOLUTION_RECORD.md`, `PRD-006_STAGE7_FREEZE_READINESS.md`, `PRD-006_STAGE7_READINESS_AUDIT.md`, `PRD-006_VERIFICATION_REPORT.md`, `PRD-006_NUMBERING_RECONCILIATION.md`, `PRD-006_FREEZE_BLOCKER_CLASSIFICATION.md`, `PRD-006_AUTHORITY_DETERMINATION.md`, `PRD-006_MASTER_GOVERNANCE_OPERATION_RECORD.md`, and the remaining `PRD-006_*` decision, approval and attempt records | v1.0–v1.2 | Review, decision and correction history — Stage 3 and Stage 4 reviews, two failed freeze/ARB **attempts preserved as records of refusal**, gap decision packs, and the numbering reconciliation from a drafting-time `PRD-008`. **Several are deliberately NOT rewritten** to match the outcome, on the `ADR-0018` §3.4 / `ADR-0019` §3.6 precedent that independently verified bytes are preserved rather than tidied. Historical records, **not part of the specification** |

| `30-product/revenue-finance/PRD-008_REVENUE-AND-FINANCE.md` | **v1.7** | **Authoritative — the Revenue & Finance baseline** for `BC-05` Fee & Payment, 43 chapters. Declares **134 obligations** (`FEE-FR` 62 · `FEE-BR` 28 · `FEE-INV` 10 · `FEE-EVT` 3 · `FEE-XC` 23 · `FEE-PO` 8), **98** acceptance criteria and **10** invariants — chiefly `FEE-INV-002` *(a `FeeDue` amount equals its creation snapshot, for life)*, `FEE-INV-005` *(no two confirmed payments share an idempotency key or gateway reference)* and `FEE-INV-009` *(`balance = Σ dues − Σ receipts`, never stored independently)*. Holds **no** payment-gateway credential and names **no** vendor — `FEE-XC-005`/`FEE-XC-006` make both **impossible**, not merely absent. Admitted by `ADR-0049`. ⚠ **`FROZEN` is not `VERIFIED`**: **17 `FEE-GAP-*` remain OPEN** and **20 of 83** implementation tasks are **BLOCKED** on them. ⚠ Carries a known **Stage 2** defect admitted with the document: `FEE-RSK-*` is registered in `TRACEABILITY_MATRIX.md` §2G but **not declared in its own §0.2** |
| `30-product/revenue-finance/PRD-008_STAGE3_CONFERRAL.md` | **v1.0** | Validation record — the **Stage 3 conferral**, **verdict ✅ ALIGNED, 6 of 6** checks, **CONFERRED** under Architecture Reviewer authority. Upholds the `E-27` rejection and **records two provenance defects rather than correcting them in place**. **Not part of the specification** |
| `30-product/revenue-finance/PRD-008_STAGE4_CONFERRAL.md` | **v1.0** | Validation record — the **Stage 4 conferral**, **verdict ✅ PASSED, C1–C7, 0 failures**, **CONFERRED** under Requirements Reviewer authority. Both rejected findings (`RJ-1`, `RJ-2`) are **upheld with reasons** per `PRD_LIFECYCLE.md` L104, and one arithmetic defect in the evidence (**8** first-cell foreign identifiers measured as **12**) is **recorded, not edited into the evidence**. **Not part of the specification** |
| `30-product/revenue-finance/PRD-008_ARCHITECTURE_ALIGNMENT_V17_SUPPLEMENT.md` | **v1.0** | Validation record — the Stage 3 architecture alignment **re-measured against v1.7** after the base record's v1.2 verdict went stale. Read **jointly** with the base record below; the base record contains **no forward link to this one**, which is a disclosed discoverability defect that this baseline row exists to remedy. **Not part of the specification** |
| `30-product/revenue-finance/PRD-008_ARCHITECTURE_ALIGNMENT.md`, `PRD-008_REQUIREMENTS_REVIEW.md` | v1.2, v1.0 | Validation records — the **v1.2** alignment record (verdict measured against superseded bytes; **deliberately left stale** and retained verbatim as evidence) and the Stage 4 requirements review. **Not part of the specification** |
| `30-product/tenancy/PRD-013_TENANCY.md` | **v0.1** | **Authoritative — the Tenancy baseline** for **`BC-19` Tenancy** `[GENERIC]`, the **first `FOUNDATIONAL`-tier product specification frozen** (BC Map L271). Owns exactly **one aggregate**, `Tenant` (`TEN-FR-004`), plus context ownership of `BC-19` — the two-rights split of `ADR-0013` §1.5. Declares **63 identifiers** across eight registers: **51 in force** (FR 18 · BR 2 · INV 2 · EVT 2 · XC 10 · CFG **0, declared empty** · AC 13 · GAP 4) and **12 retired, never reusable**. **34 obligations**, event surface **closed at two** by `TEN-XC-009`, **0** integration edges added. ⚠ **Admitted at v0.1 — the first v0.x document in this baseline** — because `ADR-0020` §4 item 2 admits *"each at the version that was verified"* and **freeze does not renumber**; every gate measured `688239cb…ba9c`. ⚠ **`FROZEN`, not `VERIFIED`: 0 of 30 tasks and 0 of 13 acceptance criteria are proven by a test.** Four `TEN-GAP-*` admitted **OPEN**. Two **code** defects carried, not fixed — `D-013-01`, `D-013-02` (`ADR-0050` §3). Conferred by **`ADR-0050`** |
| `30-product/tenancy/PRD-013_ARCHITECTURE_ALIGNMENT_SUPPLEMENT.md` | **v1.0** | Validation record — the six Stage 3 checks **re-measured against `688239cb…ba9c`** after the base record's verdict went stale at `fcd99ff7…f97e`. Verdict ✅ **ALIGNED — 5 PASS + 1 CONDITIONAL** (`H-1`, a pre-existing BC Map §7 taxonomy gap affecting `BC-20`→`BC-21` identically). Read **jointly** with the base record below, which contains **no forward link to this one** — a disclosed discoverability defect this row exists to remedy, exactly as `PRD-008`'s supplement row does. **Not part of the specification** |
| `30-product/tenancy/PRD-013_ARCHITECTURE_ALIGNMENT.md` | v1.0 | Validation record — the Stage 3 alignment record, verdict ✅ **ALIGNED, 5 of 6 + 1 CONDITIONAL**, measured at `fcd99ff7…f97e`. **Verdict measured against superseded bytes; deliberately left stale and retained verbatim as evidence** — rewriting its hash would make it claim a review that did not happen. **Not part of the specification** |
| `30-product/tenancy/PRD-013_STAGE5_CONFERRAL.md`, `PRD-013_STAGE6_IMPLEMENTATION_TASKS.md`, `PRD-013_STAGE7_FREEZE.md` | v1.0 | Stage 5, 6 and 7 gate records — traceability **CONFERRED** (both checkers exit 0, 0 collisions both directions), implementation tasks **verdict A, 13 of 13 gates**, and the freeze record. **Not part of the specification; the Stage 7 record confers nothing — the §3.3 row above does** |
| `30-product/tenancy/PRD-013_STAGE7_BLOCKER.md` | v1.0 | **SUPERSEDED by `ADR-0050` §5** and retained rather than deleted. Returned ⛔ BLOCKED on two prerequisites; blocker 1 (*"the Product Owner must release v0.1 to a version before freeze"*) is **withdrawn as an invented rule** — it inferred a prohibition from the absence of v0.x precedent and would have required the very renumbering `ADR-0020` §5 forbids. Retained as evidence of the reasoning, on the `ADR-0034` precedent that a superseded verdict is kept beside the new one. **No authority** |
| `30-product/audit/PRD-016_AUDIT_TRAIL.md` | **v0.1** | **Authoritative — the Audit Trail baseline** for **`BC-24` Audit Trail** `[GENERIC]`, the **first CAPABILITY & GENERIC band context frozen** (BC Map L259–267; measured — none of the eleven contexts in that band was owned by any of the nine previously frozen PRDs). Owns exactly **one aggregate**, `AuditEntry` (`AUD-FR-004`), plus context ownership of `BC-24` — the two-rights split of `ADR-0013` §1.5. Declares **58 identifiers** across eight registers, **0 retired**: FR 16 · BR 3 · INV 4 · **EVT 0, declared empty** · XC 12 · **CFG 0, declared empty** · AC 18 · GAP 5. **35 obligations**, **0** integration edges added — `E-20` (BC Map L329) is consumed, not created. **`BC-24` publishes nothing**: it is a producer in **zero** BC Map §9 rows and a consumer in **25**, so `AUD-EVT-*` is empty as a *finding*, not an omission. **`AUD-CFG-*` is empty because no Rank 1–7 authority supplies a bound** — `MP-NFR-10` assigns retention and legal hold to SECURITY + DATA Governance and `Q-04` records the retention period as *"Open — needs counsel"*, so a default here would have been a legal determination by a document with no standing to make one (`ADR-0051` §2.4). The audit **principles** remain `AU-1`…`AU-9` in the **frozen** Authentication PRD §10.4, cited and **not restated**; `AUTH-10.26` is the authority by which this module owns storage, retention and query. ⚠ **Admitted at v0.1 — the second v0.x document in this baseline**, on the `ADR-0050` §2.2 precedent, because `ADR-0020` §4 item 2 admits *"each at the version that was verified"* and **freeze does not renumber**; **all five gate records measured the same bytes `281f15e8…f339be`**, so **no alignment supplement was required and none was written** — unlike `PRD-008` and `PRD-013`. ⚠ **`FROZEN`, not `VERIFIED`: 0 of 30 tasks and 0 of 18 acceptance criteria are proven by a test**, and acceptance coverage is **27/35 = 77.1%** with the **8** uncovered obligations **named** in the PRD's own §9 rather than hidden. Five `AUD-GAP-*` admitted **OPEN**. Three **code** defects carried, not fixed — `D-016-01` (a query surface with no tenant argument), `D-016-02` (a silent 1970 epoch default for the audit timestamp), `D-016-03` (`ADR-0051` §3). Conferred by **`ADR-0051`** |
| `30-product/audit/PRD-016_STAGE1_DISCOVERY.md`, `PRD-016_ARCHITECTURE_ALIGNMENT.md`, `PRD-016_STAGE4_REQUIREMENTS_REVIEW.md` | v1.0 | Stage 1, 3 and 4 gate records — discovery (8 findings), architecture alignment **✅ ALIGNED 6 of 6** with **nine candidate findings rejected with evidence** (`AL-R1`…`AL-R9`), and the requirements review **✅ PASS 6 of 6** with four findings rejected (`S4-R1`…`R4`) and one **instrument** defect recorded rather than concealed (`S4-D-01`, an ellipsis-range parser blind spot). All three measured `281f15e8…f339be` — **the current bytes**, which is why no supplement exists. **Not part of the specification** |
| `30-product/audit/PRD-016_STAGE5_CONFERRAL.md`, `PRD-016_STAGE6_IMPLEMENTATION_TASKS.md` | v1.0 | Stage 5 and 6 gate records — traceability **CONFERRED** (matrix §2J; **two independent checkers**, both exit 0, 0 collisions both directions) and implementation tasks **verdict A — PASS**, `G1`…`G8`, `IMPL-900`…`929`, **35/35 = 100.0%** of obligations claimed. The Stage 6 record discloses two things a tidier record would have dropped: the **`IMPL-900` occurrence-vs-occupancy** judgement (a string scan reported `IMPL-900` as taken; all three hits proved to be *"Unallocated"* boundary labels, and obeying the scanner would have stranded the whole `IMPL-9xx` decade) and **its own gate failure on first run** — 23 messages from one 7-cell table row, where **the document was wrong and the checker was right**. **Not part of the specification; neither record confers freeze — the §3.3 row above does** |
| `30-product/entitlement/PRD-014_ENTITLEMENT.md` | **v0.1** | **Authoritative — the Entitlement baseline** for **`BC-21` Entitlement** `[GENERIC]`, admitted 2026-08-19 by **`ACCEPTED` [`ADR-0052`](adr/ADR-0052-entitlement-prd-v0.1-baseline.md)** under **`BASELINE-2026-08-19-C`**. **88 identifiers** across **8 registers** — **6 populated, 2 declared EMPTY** — **26 acceptance criteria**, **54** Class A obligations, coverage admitted at its measured **51/54 = 94.4%** rather than rounded up. **1 aggregate** (`EntitlementSet`), exactly the BC Map **L383** grant; **0 events published** (BC Map L435–437 gives `BC-21` consumers only); **0 new `E-*` edges** (§7 L292 — an edge not in the table *"does not exist"*). ⚠ **`ENT-EVT-*` and `ENT-CFG-*` are admitted EMPTY rather than populated**, because `BC-21` publishes nothing and **no authority bounds any entitlement parameter** — a timeout, grace period and fail disposition were each **refused rather than invented**, `Q-03` and `Q-06` being OPEN at BC Map §13 L542/L545. ⚠ **The version is deliberately NOT incremented** (`ADR-0020` §4 item 2, *"Freeze confers status; it does not renumber"*) — the **third** v0.x admission after `PRD-013` and `PRD-016`. ⛔ **`FROZEN` is not `VERIFIED`: 0 of 26** criteria proven and **0 of 30** `IMPL-1000`…`1029` tasks implemented. ⛔ **All 8 `ENT-GAP-*` admitted OPEN**; `H-1`, `Q-03`, `Q-06` **left undecided** as belonging to other authorities. ⛔ **`D-014-01`…`D-014-06` NOT fixed** — six code contradictions in the pre-existing `EntitlementService` scaffold carried to Stage 8, including a **direct entitlement write in `seed.dart` L95–96** that Matrix `X-11` and manifest `BZ-1` forbid; **no file under `lib/`, `packages/` or `test/` was touched** |
| `30-product/entitlement/PRD-014_STAGE1_DISCOVERY.md`, `PRD-014_ARCHITECTURE_ALIGNMENT.md`, `PRD-014_STAGE4_REQUIREMENTS_REVIEW.md` | v1.0 | Stage 1, 3 and 4 gate records — discovery **PASS** (`BC-21` identified and **unowned**; 14 findings, 12 rejected), architecture **⚠️ CONDITIONAL PASS** (8 findings, 11 rejected) and requirements **PASS 6/6** (13 deferrals, each with a reason and a named owner). ⚠ **The CONDITIONAL verdict is admitted as CONDITIONAL, not rounded to PASS**: the condition is the **inherited `H-1`** hazard `PRD-007` raised at its own L431 and `PRD-013` inherited, both *before* this module existed, and the backlog is written so `IMPL-1026` is satisfiable under **either** resolution. **Not part of the specification; neither record confers freeze — the §3.3 row above does** |
| `30-product/entitlement/PRD-014_STAGE5_CONFERRAL.md`, `PRD-014_STAGE6_IMPLEMENTATION_TASKS.md` | v1.0 | Stage 5 and 6 gate records — traceability **CONFERRED** (matrix **v1.15 §2K**; **two independent checkers**, both exit 0, 0 collisions in three directions) and implementation tasks **verdict A — PASS** (`IMPL-1000`…`1029`, 30 tasks, **54/54 = 100.0%** of obligations claimed, **6 of 6** `D-014-*` scheduled). Both records disclose what a tidier pair would have dropped: **nine self-inflicted instrument defects** (`S5-C-01`…`06`, `S6-C-01`…`04`), two of them caught by a vacuity guard catching **its own author's bad input**; one case where **the checker was wrong and the document right** — check 10 searched the Discharges cell while the `ENT-AC` span sat one column away, so trusting it would have edited a correct document to satisfy a defective instrument; and a **`-1` printed in a column of counts**, a number that counted nothing. **13 mutations injected across the two stages, all 13 caught**, every document restored byte-identical. ⚠ Also disclosed rather than repaired: `PRD_LIFECYCLE.md`'s **stale** `IMPL-227+` table (worked around by enumerating **489** real identifiers) and matrix **§2J.1's false `FR-*` location claim** (`S5-F-01`). **Not part of the specification; neither record confers freeze — the §3.3 row above does** |
| `30-product/configuration/PRD-023_SETTINGS_AND_CONFIGURATION.md` | **v0.1** | **Authoritative — the Settings & Configuration baseline** for **`BC-25` Configuration** `[GENERIC]`, admitted 2026-08-20 by **`ACCEPTED`** [`ADR-0053`](adr/ADR-0053-configuration-prd-v0.1-baseline.md) under **`BASELINE-2026-08-20-A`**. **`BC-25` is the third and last `FOUNDATIONAL`-band context to gain a Rank 3 specification** — BC Map **L271** places `BC-18`, `BC-19` and `BC-25` in that band, and with `BC-18` held by the Authentication PRD and `BC-19` by `PRD-013`, **the band is now 3 of 3 covered**. **180 identifiers** across **8 registers** — **6 populated, 2 declared EMPTY** (FR 82 · BR 11 · INV 4 · **EVT 0** · XC 16 · **CFG 0** · AC 59 · GAP 8), all contiguous, **0 duplicates, 0 collisions in three directions**; **113** Class A obligations; **59** acceptance criteria. Owns the six items `ADR-0017` **§3.1** fixed and **no seventh**: the platform → tenant → library → branch → user hierarchy, feature flags with the `BC-21` boundary, branding **values only**, secret **references only**, the **`E-19`** typed-accessor contract, and `LCFG-*` resolution semantics — *"`PRD-023` owns the resolution machinery, **not** the value list."* **0 aggregates asserted** (`CNF-GAP-001` is a live Rank 3 vs Rank 4 conflict: `Library_PRD_v1.md` **L188** names `LibrarySettings`, BC Map §8's 17 rows grant `BC-25` none, and **the PRD takes neither side**). **0 events published, 0 `E-*` edges added, 0 configurables absorbed** from the 104-item census owned by eight frozen PRDs. ⚠ **Two registers admitted EMPTY rather than populated, as findings:** `CNF-EVT-*` because `BC-25` is a producer in **zero** BC Map §9 rows, so minting an event would create an edge **L292** says *"does not exist"*; `CNF-CFG-*` because this module owns the *machinery*, and a numbered member would have duplicated an owned value or invented a 105th — **the fourth declared-empty CFG register** after `TEN-`, `AUD-` and `ENT-`. Emptiness is **enforced as a test** in three checkers, not merely asserted. ⚠ **Admitted at v0.1 — the fourth v0.x admission**, per `ADR-0020` §4 item 2 (*"Freeze confers status; it does not renumber"*); the PRD's bytes were verified unchanged **five times** at `e974960…6399326`, so every gate measured the same document and **no alignment supplement was required**. ⚠ **Stage 3's verdict is UNCONDITIONAL** — stronger than `PRD-013`'s and `PRD-014`'s CONDITIONAL PASS, for one measured reason: `BC-25`'s FOUNDATIONAL band means the inherited **`H-1`** taxonomy gap does not apply to it. ⛔ **`FROZEN` is not `VERIFIED`: 0 of 59** acceptance criteria and **0 of 30** `IMPL-1100`…`1129` tasks are proven by a test. Acceptance coverage is admitted at its measured **76/113 = 67.3%**, **not rounded up** and **not conflated with the 113/113 = 100.0% task coverage** — the two are different claims, and all **16 `CNF-XC-*` are uncovered by construction** because a criterion asserting that something never happens is unfalsifiable by observation. ⛔ **All 8 `CNF-GAP-*` admitted OPEN**; **9 blockers `B-1`…`B-9`** classified with **5 ⛔ hard-external** (the missing `platform/configuration` module block, the seven missing architecture tests, UI Design System, NFR Budgets, Event Catalog). ⚠ **`S4-D-01` disclosed, not repaired** — the PRD over-declares its own uncovered set by three, which **understates** its coverage, and repairing it would change the hash four gate records are anchored to. **No file under `lib/`, `packages/`, `test/` or `web/` was touched — 0 lines** |
| `30-product/configuration/PRD-023_STAGE1_DISCOVERY.md`, `PRD-023_ARCHITECTURE_ALIGNMENT.md`, `PRD-023_STAGE4_REQUIREMENTS_REVIEW.md` | v1.0 | Stage 1, 3 and 4 gate records — discovery **PASS** (`BC-25` identified and unowned; prefix `CNF-` selected by measurement; `IMPL-1100` established next-free as measurement `M-22`), architecture **✅ ALIGNED — PASS 6 of 6, UNCONDITIONAL** (10 owned / 13 ceded / 6 edges examined, **0 created**; 8 findings accepted, **11 rejected with evidence**) and requirements **✅ PASS 6 of 6** (113/113 testable, 16/16 impossibility statements, **0 configurables minted**, 0 orphans, 0 undisclosed uncovered, 0 restatements; `D-01`…`D-06` deferred with named owners). **Not part of the specification; none of the three confers freeze — the §3.3 row above does** |
| `30-product/configuration/PRD-023_STAGE5_CONFERRAL.md`, `PRD-023_STAGE7_FREEZE.md` | v1.0 | Stage 5 and 7 gate records — traceability **CONFERRED** (matrix **v1.16 §2L**; **two independent checkers**, neither importing the other, both exit 0, **0 collisions in three directions**, reaching the same 180 by different parsing strategies) and the freeze record. Both disclose what a tidier pair would have dropped: **a third self-found instrument defect** (`S5-C-03`, a legitimate regex *test vector* mistaken for a citation, fixed narrowly and **guarded to fail loudly if the convention disappears** so the exemption cannot rot into a blanket one); **four dangling citations the author introduced while describing the mutations that prove the gate catches phantoms** — the checker was right and **the prose was fixed, not the rule**; a carried-forward `M11` miss **re-run and proven to be a harness defect rather than a gate blind spot** (`sed` addressing a table row where §12.1 defines requirements as prose), re-tested as M11a/M11b and both caught, **12/12**; and a regression sweep that reported **20/20 clean while contradicting 5 known failures** — root-caused to a `case … continue` resetting `$?` (`S5-F-02`), after which **exactly the 5 known pre-existing failures reappeared and nothing had been weakened**. **Not part of the specification; the Stage 7 record confers nothing — the §3.3 row above does** |
| `30-product/file-media/PRD-017_FILE_AND_MEDIA.md` | **v0.1** | **Authoritative — the File & Media baseline** for **`BC-29` File & Media** `[GENERIC]`, admitted 2026-08-20 by **`ACCEPTED`** [`ADR-0054`](adr/ADR-0054-file-media-prd-v0.1-baseline.md) under **`BASELINE-2026-08-20-B`**. **`BC-29` is the first of the two `Platform Services` contexts to gain a Rank 3 specification** — BC Map §3.3's subdomain column places `BC-29` File & Media and `BC-30` Offline Sync in that cluster, and `BC-30` is `PLANNED`. ⚠ **Neither adjacent predecessor's band phrase is reused, because both were tested and both are false here:** `PRD-016` claimed the *"first CAPABILITY & GENERIC band context frozen"* — that band (BC Map **L258–268**) holds **eleven** contexts and `BC-21` and `BC-24` are already frozen, so `BC-29` makes it **3 of 11**, an unremarkable figure stated because it is the true one; `PRD-023` claimed a FOUNDATIONAL *"3 of 3"*, and `BC-29` is not FOUNDATIONAL. **232 identifiers** across **8 registers** — **7 populated, 1 declared EMPTY** (FR 82 · BR 17 · INV 11 · **EVT 0** · XC 22 · CFG 9 · AC 78 · GAP 13), all contiguous, **0 duplicates, 0 dangling, 0 collisions**; **132** Class A obligations; **78** acceptance criteria. ⚠ **`FIL-EVT-*` is admitted EMPTY as a finding** — `BC-29` is a producer in **zero** BC Map §9 rows, so minting an event would create an edge **L292** says *"does not exist"*; the **fifth** declared-empty EVT register. ⚠ **`FIL-CFG-*` is POPULATED at nine, unlike all four v0.x predecessors**, whose CFG registers were each declared empty for want of a bounding authority — size limits, retention windows, MIME allow-lists and signed-URL TTLs are bounded by authorities this module may read, so nine members were **derived rather than invented and rather than refused**. ⛔ **Student-to-Student Text + File/Media Sharing is retained in V1 scope** on instruction, and the specification is **honest about being unable to serve it**: BC Map **L331** lists `E-22`'s consumers as `BC-01`, `BC-10`, `BC-14` and **not** `BC-12` Messaging, so the one context that actually needs a message attachment cannot lawfully obtain one. Stage 3 tested necessity **per context** and narrowed the gap precisely — **`BC-12` YES** (the minimal sufficient consumer), **`BC-11` Social Graph NO**, **`BC-13` Trust & Safety NO** (`E-14` is outbound). This is **`FIL-GAP-012`**, OPEN, and implementation blocker **`B-11`**, blocking **`IMPL-1230`…`IMPL-1236`**. ⚠ **`ADR-0016` is the exact one-cell precedent for the required amendment and `ADR-0054` §3.2 DECLINES to exercise it** — BC Map amendment belongs to the **Architecture Owner** and the conferred authority here is Governance Owner; the gap is **routed, not resolved**, on the measured `ATT-GAP-010` precedent (`PRD-006` **L1564**/**L2973**) that a freeze may carry an open `E-22` consumer gap transparently. ⚠ **Admitted at v0.1 — the fifth v0.x admission**, per `ADR-0020` §4 item 2 (*"Freeze confers status; it does not renumber"*). ⚠ **Unlike `PRD-016` and `PRD-023`, an alignment supplement WAS required and WAS written** — see the supplement row below. ⛔ **`FROZEN` is not `VERIFIED`: 0 of 78** acceptance criteria and **0 of 40** `IMPL-1200`…`1239` tasks are proven by a test; **7 of 7** required architecture tests are missing (`B-2`). Acceptance coverage is admitted at its measured **94/132 = 71.2%**, **not rounded up** and **not conflated with the 132/132 = 100.0% task coverage**; the **38** uncovered obligations are enumerated by class (**XC 17 · FR 17 · BR 3 · INV 1**) with **0 undisclosed**. ⛔ **All 13 `FIL-GAP-*` admitted OPEN.** **0 aggregates asserted beyond the BC Map grant, 0 events, 0 `E-*` edges added, 0 BC Map cells changed, 0 requirements moved.** **No file under `lib/`, `packages/`, `test/` or `web/` was touched — 0 lines** |
| `30-product/file-media/PRD-017_ARCHITECTURE_ALIGNMENT_SUPPLEMENT.md` | **v1.0** | Validation record — the six Stage 3 checks **re-measured against the current bytes `4ebf5d07…d4e8`** after the base record's verdict was found anchored to superseded bytes `85adb6fa…cb44e`. Verdict ✅ **PASS — CONDITIONAL, 6 of 6**, **unchanged**, and the reason it is unchanged is measured rather than assumed: the drift is **`+2 FIL-AC-*` and nothing else** (230 → 232 distinct identifiers; **`FIL-AC-077`, `FIL-AC-078` added, none removed**), with every Class A register **identical in membership** (FR 82 · BR 17 · INV 11 · EVT 0 · XC 22), so no architectural verdict rested on bytes that moved. ⚠ **This supplement exists because a convenient precedent was tested and falsified.** `PRD-016` and `PRD-023` each record that *"all five gate records measured the identical hash, so no supplement was required and none was written"*; writing that sentence here would have been fluent and **false**. Extracting every hash citation from every PRD-017 gate record measured **three of four stale** — base alignment L6, and the Stage 4 record's own self-disclosed before/after pair. This is the `PRD-008`/`PRD-013` case, **not** the `PRD-016`/`PRD-023` case. Read **jointly** with the base record below, which contains **no forward link to this one** — a disclosed discoverability defect this row exists to remedy, exactly as `PRD-008`'s and `PRD-013`'s supplement rows do. The general instrument defect — **nothing mechanically checks whether a gate record's cited hash still matches its subject** — is **`GCP-22`** in §6, carried not repaired. **Not part of the specification** |
| `30-product/file-media/PRD-017_ARCHITECTURE_ALIGNMENT.md`, `PRD-017_STAGE4_REQUIREMENTS_REVIEW.md`, `PRD-017_STAGE5_CONFERRAL.md` | v1.0 | Stage 3, 4 and 5 gate records — architecture alignment **✅ PASS — CONDITIONAL, 6 of 6** (`E-22` narrowed to `BC-12` alone as accepted finding `S3-A-01`; **0 edges, 0 cells, 0 ports, 0 ADRs changed**), the requirements review (**14 adversarial categories**, 6 genuine defects fixed, no unrelated frozen document touched) and traceability **CONFERRED** (matrix **v1.17 §2M**; **two independent checkers**, neither importing the other, both exit 0, **479** citations resolved across **24** scopes, **0** dangling, **12/12** mutants killed). ⚠ **The Stage 5 record explicitly does NOT certify architecture alignment while `FIL-GAP-012` is OPEN** — a checker asserts that refusal, so the conferral cannot silently drift into a claim it was not entitled to make. ⚠ **The first two records' cited hashes are STALE and are deliberately left stale** — rewriting a hash would make a record claim a review of bytes it never saw; the supplement above is the answer, and `S4-D-06` is recorded in `ADR-0054` §3.1 rather than back-dated into the Stage 4 record it belongs to. **Not part of the specification; none of the three confers freeze — the §3.3 row above does** |
### 3.4 Configuration and implementation

| Document | Version | Status |
|---|---|---|
| `20-configuration/CONFIGURATION_GUIDE.md` | v1.0 | Normative for operations |
| `40-implementation/DEVELOPER_HANDOFF.md` | v1.0 | Entry point for developers |
| `40-implementation/IMPLEMENTATION_ROADMAP.md` | v1.0 | Sequenced work |
| `40-implementation/AUTHENTICATION_IMPLEMENTATION_CHECKLIST.md` | v1.0 | Per-requirement checklist |
| `40-implementation/TASK-D10-remove-demo-surfaces.md` | v1.0 | Release-blocking task |
| `40-implementation/DEFINITION_OF_DONE.md` | v1.1 | Merge and release gates. Extended to Library Management; the stale `AR-4` *"do not invent"* row replaced |
| `40-implementation/TRACEABILITY_MATRIX.md` | **v1.1** | Requirement → artefact mapping |
| `40-implementation/LIBRARY_IMPLEMENTATION_TASKS.md` | **v1.0** | `IMPL-100`…`IMPL-127` with acceptance criteria and checklists |
| `40-implementation/STUDENT_IDENTITY_IMPLEMENTATION_TASKS.md` | **v1.0** | `IMPL-200`…`IMPL-226` with acceptance criteria and checklists |
| `40-implementation/seat-management/PRD-007_IMPLEMENTATION_TASKS.md` | **v1.0** | `IMPL-500`…`IMPL-599` — **100 tasks in ten waves**, `443/443 = 100.0%` of obligations claimed, 0 cycles, critical path 18. Backlog record, **not ranked** |
| `40-implementation/seat-management/PRD-007_DEVELOPER_GUIDE.md` | **v1.1** | Developer entry point for `BC-04` Seating, derived from the frozen PRD. **Navigational and explanatory — carries no requirement and no authority.** v1.1 corrects one wording defect only (`GCP-10`, §6) |
| `40-implementation/configuration/PRD-023_IMPLEMENTATION_TASKS.md` | **v1.0** | `IMPL-1100`…`IMPL-1129` — **30 tasks in three waves**, with **`IMPL-1130`…`IMPL-1199` declared as contiguous growth reserve** (allocation rule 2, following the `ADR-0052:279` precedent). **113 of 113 = 100.0%** of Class A obligations claimed — **computed by a third checker, not asserted**, and the figure was **corrected downward-then-fixed on first run**: the gate found `CNF-FR-059` claimed by no task and the document publishing **113 against a recomputed 112**, so a task was assigned rather than the number adjusted. Publishing 113 over a true 112 would have reproduced the `PRD-006` v1.0 failure **in the very document whose §6.2 names it**. Records **9 blockers `B-1`…`B-9`** (5 ⛔ hard-external), **11 forbidden tasks** so no speculative work is created, and §3.4's statement of the `IMPL-015`/`016` relationship **without duplicating `PRD-001`'s two tasks and without closing `CNF-GAP-005`**. Backlog record, **not ranked** |
| `40-implementation/file-media/PRD-017_IMPLEMENTATION_TASKS.md` | **v1.0** | `IMPL-1200`…`IMPL-1239` — **40 tasks in four waves of ten**, with **`IMPL-1240`…`IMPL-1299` declared as contiguous growth reserve**, following the `ADR-0052:279` / `PRD-023` precedent. The range was established **mechanically by two independent routes** that agree: a repo-wide scan of every `IMPL-*` citation (**553** distinct, `MAX = IMPL-1200`, which proved on inspection to be a **reserve boundary marker** rather than an allocated task) and the explicit reservation statement at `PRD-023_IMPLEMENTATION_TASKS.md` **L89**. ⚠ **The Rank 1 `PRD_LIFECYCLE.md` **L147–153** allocation table says `IMPL-227+` and was NOT followed** — obeying it would have collided with four separate reserved bands (`830`–`899`, `930`–`999`, `1030`–`1099`, `1130`–`1199`). The stale table is **carried as a finding, not repaired**, because amending a Rank 1 document is a different act from allocating a range. **132 of 132 = 100.0%** of Class A obligations claimed, **0 phantom claims**, computed by a checker rather than asserted. ⛔ **`IMPL-1230`…`IMPL-1236` — seven tasks — are admitted BLOCKED** by **`B-11`**, the `E-22`/`BC-12` gap: they are the Student-to-Student sharing integration tasks, and they are written so that **no work begins on an edge that does not yet lawfully exist**. **Student-to-Student sharing tasks appear only where `PRD-017` actually owns the work** — storage, metadata, signed-URL issue, scan and revocation — and **no `PRD-021` implementation work is duplicated**. Backlog record, **not ranked** |

### 3.5 Archived — no authority

| Document | Reason |
|---|---|
| `90-archive/authentication-v1/Authentication_PRD.md` | v1 stub; eleven empty slots; superseded by v2.0 |
| `90-archive/authentication-v1/Authentication_PRD_Developer_Guide.md` | Written against the absent v1; every claim of absence is now false |
| `90-archive/authentication-v1/01-*.md` … `11-*.md` | The eleven empty slots |

Archived documents are **retained for history and must not be cited as authority.** Each carries a header saying
so.

---

## 4. Precedence

When two documents disagree, the higher rank wins. This order is not negotiable at implementation time.

| Rank | Source | Scope |
|---|---|---|
| **1** | `MASTER_PRD.md` global rules — `MP-GBR-*`, `MP-CON-*`, `MP-DEP-*` | Platform-wide. Outranks every module PRD |
| **2** | Accepted ADRs — **41 accepted of 54 files**, measured not assumed: `ADR-0001`…`ADR-0020`, `ADR-0032`…`ADR-0037`, `ADR-0039`, `ADR-0040`, `ADR-0042`…**`ADR-0054`**. *(Re-derived on 2026-08-20 by classifying **all 54** files' own `Status` rows on the leading declared token, stripping `**` and backticks — **53 / 40 / 13 / 0 before `ADR-0054`, 54 / 41 / 13 / 0 after**, each sum equalling the file count — not by incrementing. **Only ONE number moves — accepted 40 → 41 — and `proposed` is deliberately unchanged at 13**; no ADR was promoted, demoted, superseded or deprecated, and **`ADR-0022`, which frames the same `E-22` question for `BC-03`, remains `Proposed` and was NOT promoted to settle `FIL-GAP-012` by convenience.** ⚠ **This cell was CURRENT when this admission began** — a positive result worth recording, because it read *\"38 accepted of 51 files\"* one admission ago: `GCP-16`/`GCP-19`'s defect had recurred **three** times in this one cell, the `PRD-023` pass repaired it, **and the repair held.**)* *Prior text retained verbatim:* **40 accepted of 53 files**, measured not assumed: `ADR-0001`…`ADR-0020`, `ADR-0032`…`ADR-0037`, `ADR-0039`, `ADR-0040`, `ADR-0042`…**`ADR-0053`**. *(Re-derived on 2026-08-20 by classifying all files' own `Status` rows on the leading declared token, stripping `**` and backticks — **52 / 39 / 13 / 0 before `ADR-0053`, 53 / 40 / 13 / 0 after** — not by incrementing. ⚠ **This cell was measurably stale before `ADR-0053` extended it**, reading *"38 accepted of 51 files"* and enumerating only to `ADR-0051` while `ADR-0052` was accepted: **`GCP-16`/`GCP-19`'s defect recurring for the THIRD time in the one cell those two entries were opened to repair.** It is corrected here because this admission must extend the same cell, and it is logged as **drift 2** in `ADR-0053` §8.1 rather than presented as untouched. **Only ONE number moves — accepted 39 → 40 — and `proposed` is deliberately unchanged at 13**; no ADR was promoted, demoted or superseded.)* *Prior text retained verbatim:* **38 accepted of 51 files**, measured not assumed: `ADR-0001`…`ADR-0020`, `ADR-0032`…`ADR-0037`, `ADR-0039`, `ADR-0040`, `ADR-0042`…**`ADR-0051`**. (`ADR-0021`…`ADR-0031`, `ADR-0038`, `ADR-0041` are **`Proposed` — not binding**, **13** of them) *(Was stated as "36 accepted of 50 files" while its own enumeration already ran to `ADR-0050`. Re-derived on 2026-08-19 by classifying all 51 files' own `Status` rows on the leading declared token, stripping `**` and backticks — **51 / 38 / 13 / 0, summing to 51** — not by incrementing. The enumeration was right and the leading number was behind by **one** — `ADR-0050` was added and enumerated here without the count advancing, the same `GCP-16` defect class this row has already carried once. Logged as part of **`GCP-19`**, §6.)* | Structural decisions |
| **3** | **Authentication PRD v2.0** | Everything inside `BC-18` |
| **3** | **Library PRD v1.1** + §14A + §14B + Invitation Security Specification | Everything inside the Library Management domain |
| **3** | **Student Identity & Profile PRD v1.0** + Alignment Report | Everything inside `BC-10` Global Person Identity |
| **3** | **Student Management PRD v1.2** + Alignment Report | Everything inside `BC-01` Enrollment, and the Library Members Directory read composition |
| **3** | **Membership Management PRD v1.4** | Everything inside `BC-02` Membership |
| **3** | **Seat Management PRD v1.0** | Everything inside `BC-04` Seating |
| **3** | **Attendance Management PRD v1.9** | Everything inside `BC-03` Attendance |
| **3** | **Revenue & Finance PRD v1.7** | Everything inside `BC-05` Fee & Payment |
| **3** | **Tenancy PRD v0.1** | Everything inside `BC-19` Tenancy |
| **3** | **Audit Trail PRD v0.1** | Everything inside `BC-24` Audit Trail |
| **3** | **Settings & Configuration PRD v0.1** | Everything inside `BC-25` Configuration — the settings hierarchy, feature-flag resolution, branding **values**, secret **references**, the `E-19` typed-accessor contract and `LCFG-*` resolution semantics. **The resolution machinery, not the value list** |
| **3** | **File & Media PRD v0.1** | Everything inside `BC-29` File & Media — file and media **storage, metadata, upload and download, validation, signed-URL issue and expiry, virus scanning, revocation and lifecycle**, tenant-scoped throughout. **The bytes and the reference to them, not the meaning of the thing referenced**: a consuming context holds a `FileRef` and never bytes or a raw storage path (`E-22`, BC Map **L331**). ⛔ **`BC-12` Messaging is NOT an `E-22` consumer**, so the V1 Student-to-Student File/Media sharing capability is **specified but not yet servable** — `FIL-GAP-012`, OPEN, routed to the Architecture Owner |
| **4** | Bounded Context Map · Module Dependency Matrix | Boundaries, ownership, permitted edges |
| **5** | Architecture Rulings `AR-1`, `AR-3`, `AR-4` | Domain classifications not promoted to ADRs |
| **6** | Enterprise Architecture v2.2 | **Descriptive only.** Update it to match 1–5; never the reverse. *(Version token advanced **v2.1 → v2.2** on 2026-08-15 — `ADR-0042` §4. The rule is unchanged; only the version it names moved, and it moved **because** this rule was applied: EA **L823** was updated to match Rank 4, not the reverse.)* |
| **7** | Configuration Guide | Operating values within the envelope 1–3 define |

**A conflict is a defect.** If you find one, do not choose — raise it. The precedence order tells you which
document is *wrong*, not which one to quietly ignore.

**Rank 3 holds thirteen module baselines and they do not overlap.** *(Advanced from "eleven" — **by two, not by one** — on 2026-08-20. This sentence was itself **measurably stale**: `PRD-023` Configuration was admitted earlier the same day and added to §3.3 without this sentence moving, so it read "eleven" while §3.3 already held **twelve**. That is the §7 rule 3 derived-statement defect **recurring in the very sentence the `PRD-014` admission advanced**, and it is repaired here — with the recurrence recorded, not smoothed over — because this admission must extend the same sentence. `PRD-023` is the twelfth and **`PRD-017` File & Media is the thirteenth**, admitted by `ACCEPTED` `ADR-0054`.)* **`BC-29` File & Media is governed by `PRD-017` alone**, and it is the **first of the two `Platform Services` contexts** — BC Map §3.3's subdomain column pairs `BC-29` with `BC-30` Offline Sync, and `BC-30` is `PLANNED`. ⚠ **The adjacent band claims were tested and are NOT reused:** `BC-29` sits in the CAPABILITY & GENERIC band (BC Map **L258–268**, **eleven** contexts) where `BC-21` and `BC-24` are already frozen, so `BC-29` makes that band **3 of 11** — stated because it is true, not because it is impressive. **`BC-29` and `BC-12`/`BC-14` are the boundary most likely to be misread as an overlap, and it is not one**: `PRD-017` owns the *bytes and the reference*, while what a file **means** — a message attachment, a shared content post, a profile photo — belongs to the consuming context. The temptation this pass explicitly refused was to route student-to-student sharing through **`BC-14` Content Sharing**, which *is* already an `E-22` consumer and would have made the gap disappear: **a message attachment is not a content post**, and `ADR-0054` §3.3 records the move as *"architecturally clever and factually wrong"*. **The one live tension is `FIL-GAP-012`, OPEN and deliberately undecided by this freeze** — `BC-12` Messaging needs `E-22` and does not hold it; settling it means amending a **Rank 4** document, which is the Architecture Owner's act and not the Governance Owner's. *(The preceding eleven-baseline statement, preserved:* **Rank 3 holds eleven module baselines and they do not overlap.** *Advanced from "ten" on 2026-08-19 when `PRD-014` Entitlement was admitted by `ACCEPTED` `ADR-0052`. `PRD-014` is the eleventh. The count was advanced **in the same commit as the §3.3 row**, per §7 rule 3 — a baseline that does not match the repository is worse than no baseline — and the prior wording is retained verbatim below rather than overwritten.)* **The preceding ten-baseline statement, preserved:** **Rank 3 holds ten module baselines and they do not overlap.** *(Was stated as "nine" before `PRD-016` Audit Trail was admitted 2026-08-19 by `ADR-0051`; and as "seven" before that, while §3.3 already carried `PRD-008` — corrected and logged as `GCP-16`. `PRD-013` is the ninth, `PRD-016` the tenth.)* **`BC-24` Audit Trail is governed by `PRD-016` alone**, and it is the **first context in the CAPABILITY & GENERIC band** (BC Map L259–267) to hold a frozen product specification — measured, not assumed: none of the eleven contexts in that band was owned by any of the nine previously frozen PRDs. **`BC-24` and `BC-18` are the boundary most likely to be misread as an overlap, and it is not one**: the audit *principles* `AU-1`…`AU-9` live in the **frozen** Authentication PRD §10.4 and `PRD-016` **cites rather than restates them**, while `AUTH-10.26` — *"Authentication MUST NOT own audit storage, retention or query"* — is the very authority that assigns those three to `BC-24`. `BC-18` decides *what is worth recording about authentication*; `BC-24` owns *how any fact is stored, retained and queried*. Neither may decide the other's question, and the one live tension between them (`AUTH-10.27`'s *"inability to record … MUST be treated as a failure of the action"* against `E-20`'s fire-and-forget transport) is **`AUD-GAP-002`, OPEN** — deliberately undecided by the freeze, because settling it would amend one of two frozen documents without an ADR. `BC-05` Fee & Payment is governed by `PRD-008` alone, and **`BC-19` Tenancy by `PRD-013` alone**. `BC-19` is the first `FOUNDATIONAL`-tier context to hold a frozen product specification, so the tenant-partitioning contract every other context depends on is now settled at Rank 3. **`BC-19` and `BC-06` are the boundary most likely to be misread as an overlap, and it is not one**: `PRD-013` owns the `TenantOrganisation`-side tenant record and tenant-context propagation, while **`Branch` belongs to `BC-06` and `PRD-002`** — a distinction §5 fixes as a baseline term, and one `PRD-013` respects by containing **zero** occurrences of the word.

**The original seven-baseline statement, preserved:** "**Rank 3 holds seven module baselines and they do not overlap.**" The Authentication PRD governs `BC-18`; the Student
Identity PRD governs `BC-10`; the Student Management PRD governs `BC-01` Enrollment; the Membership PRD governs
`BC-02`; the Seat Management PRD governs `BC-04` Seating; the **Attendance PRD governs `BC-03` Attendance**; the Library
PRD governs Library Management. **`BC-03` and `BC-04` are the boundary most likely to be misread as an overlap, and it is not one**: `PRD-006` owns whether a student is *present*, `PRD-007` owns whether a seat is *allocated*, and the Seat Card's presence states are a **read-time composition** in `BC-04` over a projection published by `BC-03`. Seat allocation is not presence and presence is not allocation; neither document may decide the other's question. Where a Library requirement appears to constrain authentication — the preview's
authentication boundary, the invitation acceptance sequence — the Library document states *what must be true* and
`BC-18` remains the sole authority on *how it is decided*. A Library requirement can never grant, evaluate or cache
an authorization decision. That is not a precedence question; it is `X-13` and it is forbidden.

---

## 5. Terms fixed by this baseline

| Term | Means |
|---|---|
| "The Authentication PRD" | Authentication PRD **v2.0**, and nothing else |
| "The specification" (in an authentication context) | Authentication PRD v2.0 |
| "The rulings" | `ARCHITECTURE_RULINGS.md` **v1.2**, noting four are promoted to ADRs |
| "The Library PRD" | `Library_PRD_v1.md` **v1.1** *together with* §14A, §14B and the Invitation Security Specification. All four, never one alone |
| "Library" (unqualified) | **Ambiguous — do not use.** Say `TenantOrganisation` (the billing entity, `BC-19`) or `Branch` (the physical location with hours and seats, `BC-06`) |
| "The BC Map" | `LIBOORA_BOUNDED_CONTEXT_MAP.md` **v1.5** |
| "The EA" | `LIBOORA_ENTERPRISE_ARCHITECTURE.md` v2.2 *(v2.1 until 2026-08-15 — `ADR-0042` §4)* |

---

## 6. Known gaps carried by this baseline

These are **declared, not hidden.** The baseline is complete for authentication implementation and incomplete
elsewhere. Full detail and priority in `DEVELOPER_HANDOFF.md` §7.

| Gap | Effect | Priority |
|---|---|---|
| **`FIL-GAP-001` … `FIL-GAP-013`** — thirteen open questions carried by `PRD-017` File & Media | **OPEN, and admitted OPEN. All thirteen.** `ADR-0054` §4 admits every one without ratifying any (`ADR-0020` §4 item 6). The load-bearing one is **`FIL-GAP-012`**: BC Map **L331** lists `E-22`'s consumers as `BC-01`, `BC-10`, `BC-14` and **not `BC-12` Messaging**, so the V1 Student-to-Student File/Media sharing capability is **specified and not servable**. Stage 3 narrowed it precisely rather than broadly — necessity was tested **per context** and only `BC-12` needs the edge; `BC-11` Social Graph does not, and `BC-13` Trust & Safety does not because `E-14` is **outbound**. ⚠ **`ADR-0016` is the exact one-cell precedent for the fix and `ADR-0054` §3.2 declines to exercise it**: BC Map amendment is an **Architecture Owner** act and the authority conferred for this pass is Governance Owner. The gap is therefore **routed to a named owner, not resolved by a grant that does not reach it** | **P1** for `FIL-GAP-012` — it gates a **retained V1 capability** and seven implementation tasks. **P2** for the remainder |
| **`B-11`** — the same `E-22`/`BC-12` gap expressed as an implementation blocker | **OPEN.** Blocks **`IMPL-1230`…`IMPL-1236`** — seven of the forty `PRD-017` tasks, the Student-to-Student sharing integration set. They are written **BLOCKED rather than omitted**, so the backlog records that the work is known, scoped and lawfully unstartable, instead of silently dropping a capability the Product Owner retained in V1 | **P1** — Architecture Owner. One ADR amending one BC Map cell unblocks all seven |
| **`B-2`** — the seven architecture tests `TRACEABILITY_MATRIX.md` §10.3 requires do not exist | **OPEN, and pre-existing rather than introduced.** `PRD-023` carried the identical blocker one admission earlier and `PRD-017` inherits it unchanged. It is the mechanical reason **`FROZEN` is not `VERIFIED`** for this module: `flutter test test/architecture/` passes 20 of 20 tests, and **none of the seven the matrix actually mandates is among them** | **P2** — hard-external. Not fixable inside a documentation pass |
| **`GCP-22`** — **no instrument checks whether a gate record's cited subject hash still matches its subject** | **OPEN, and DELIBERATELY NOT REPAIRED.** Discovered by measurement while admitting `PRD-017`, and it is the finding that changed this pass's deliverables: **three of the four hashes cited by PRD-017's gate records were stale.** The immediate predecessors `PRD-016` and `PRD-023` both record *"all five gate records measured the identical hash, so no supplement was required and none was written"* — inheriting that sentence would have been fluent, consistent with recent precedent and **false**, so the citations were extracted and compared instead, and a **Stage 3 supplement was written** because one was genuinely required. The specific instances are answered. **The general defect is not:** `alignment_record_freshness.py` carries a hard-coded `RECORDS` table that **does not cover `PRD-017` at all**, so nothing would have caught this and nothing will catch the next one. ⚠ **The honest reading is that this was found by diligence, and diligence does not scale** — the argument is for a mechanical gate-record freshness check over every `docs/30-product/*/` record, not for more careful reading. **Repair is a tooling act needing its own change**; carried into `ADR-0054` §8.1 drift 1 and its `Opens` row | **P2** — misconfers nothing today; every stale citation in this module is disclosed in the document that carries it. **Recurrence risk is high and unbounded, because it is unmeasured** |
| **`ENT-GAP-001` … `ENT-GAP-008`** — eight open questions carried by `PRD-014` Entitlement | **OPEN, and admitted OPEN.** `ADR-0052` §4 item 7 admits all eight without ratifying any (`ADR-0020` §4 item 6). Two are upstream and named in the repository already: **`Q-03`** (fail-open vs fail-closed per feature) and **`Q-06`** (proration ownership), both OPEN at BC Map §13 **L542**/**L545** and `MASTER_PRD.md` **L675**/**L678**, which direct that each *"should become an ADR."* A value for either would have had to be **invented**, so none was | **P1** — blocks `ENT-CFG-*` being populated; no V1 effect until `BC-21` implementation starts |
| **`D-014-01` … `D-014-06`** — the pre-existing `EntitlementService` scaffold contradicts `PRD-014` in six measured ways | **OPEN, code-rank.** `lib/platform/business/business.dart` holds tier and usage state in bare maps with an ambient `tenantId` and no rebuild-from-events path; `lib/bootstrap/seed.dart` **L95–96** writes entitlement **directly**, which Matrix **`X-11`** warns makes *"revenue leakage unauditable"* and manifest assertion **`BZ-1`** forbids; and **no test file exists at all** for `platform/business`. Scheduled to `IMPL-1022`, `1000`–`1003`, `1002`, `1009`, `1029`, `1012`. **Not fixed by the freeze** — `ADR-0020` §6.2, *"Freeze authorises implementation to begin; it does not perform it"* | **P0** — 6 findings, `D-014-06` the most serious |
| **`H-1`** — the hazard that made `PRD-014`'s Stage 3 verdict CONDITIONAL | **OPEN, and inherited.** Raised by `PRD-007` at its own **L431** and inherited by `PRD-013` *before* `BC-21` was specified, so it is not a `PRD-014` defect and `PRD-014` holds no authority to resolve it. Neutralised rather than papered over: `IMPL-1026` is written to be satisfiable under **either** resolution, and no task in the 30 is contingent on the answer | **P2** — no V1 effect; needs an ADR by the Architecture Owner |
| **`S5-F-01`** — `TRACEABILITY_MATRIX.md` §2J.1 attributes a bare `FR-*` register to a location that has no `FR` column | **OPEN by choice, disclosed not repaired.** The hazard **class** is real (bare `FR-\d+` exists at `docs/README.md` L111–112, `ADR-INDEX.md` L75, `ADR-0018` L45) and §2J's **conclusion** is correct; only its stated **location** is wrong. Amending baselined prose is not a Stage 5 or Stage 7 act, and the governing precedent is that *"a stale review is corrected by a dated re-run beside it, not by editing what it concluded."* Recorded here so a future author copying that sentence does not inherit a false premise | P3 — documentation accuracy; misconfers nothing |
| **The `IMPL-*` allocation table in `PRD_LIFECYCLE.md` (Rank 1) is stale** | **OPEN, disclosed for the fourth consecutive backlog** (`PRD-008`, `PRD-013`, `PRD-016`, `PRD-014`). It ends `IMPL-227+ | Unallocated` while `IMPL-227` is in fact occupied by Student Identity growth, so trusting it would breach allocation rule 1. Every backlog since has computed its range by **enumerating real identifiers** instead — 489 of them for `PRD-014`. **Not amended:** a Rank 1 change requires an ADR **first** (§7 rule 1). That four passes have now worked around it is itself the evidence the repair is owed | P2 — needs its own ADR |
| **`GCP-20`** — **§4 of this declaration never received a Rank 3 row for `PRD-014` Entitlement** | **OPEN, and DELIBERATELY NOT REPAIRED.** Measured on 2026-08-20 while admitting `PRD-023`: `sed -n '220,258p' \| grep -c '^\| \*\*3\*\* \|'` returns **10** Rank 3 rows against §3.3's **11** module baselines, and the missing one is Entitlement. `BASELINE-2026-08-19-C` added the §3.3 row, the §6 gap rows and the §8 changelog entry, **but not the §4 row** — the identical §7 rule 3 failure already logged as `GCP-07`, in the same table. **This is the tenth instance of the derived-statement class** across six documents (`GCP-01`, `07`, `08`, `11`, `12`, `15`, `16`, `17`, `18`, `19`). **`PRD-014`'s Rank 3 authority is NOT in doubt** — §2.1 makes the §3.3 row and the `ADR-0052` acceptance operative, and both are present and correct; only the precedence table is incomplete. **Not repaired on explicit instruction**, and because repairing it would amend a *different* frozen PRD's precedence entry as a side effect of admitting `PRD-023` — the silent amendment §7 rule 1 and `PRD_LIFECYCLE.md` **L177** forbid. ⚠ **A consequence stated rather than left to inference:** because §4 is not repaired while the `PRD-023` row **is** added, §4 now holds **11** rows against §3.3's **12**, so the arithmetic gap **survives this admission by design**. Carried into `ADR-0053` §3.2 and `PRD-023_STAGE7_FREEZE.md`. **Repair is a Governance Owner act needing its own ADR** | **P2** — recurrence risk; misconfers nothing. **Tenth instance — the argument is now for a mechanical §3.3↔§4 parity check, not for more diligence** |
| **`GCP-21`** — **§3.2 records `ARCHITECTURE_RULINGS.md` at v1.2 while the file's own version row reads v1.1** | **OPEN, and deliberately NOT settled.** Measured 2026-08-20 during `ADR-0053` §8.1's pre-existing-defect search: `docs/10-architecture/ARCHITECTURE_RULINGS.md` **L6** reads `v1.1`, while §3.2 of this declaration records **v1.2**. ⚠ **Unlike `GCP-11`, which was the same shape and was repaired, this one is NOT repaired — because it is not established which of the two records is wrong.** The baseline may be recording a bump the file never received, or the file may have been reverted after a bump. **Writing either value would settle the question by guessing**, and choosing the file's value would silently retract a version this declaration has asserted since 2026-08-04. Determining which is correct requires reading `AR-*` ruling history against the ADR promotions that moved `AR-2`, `AR-5`, `AR-6` and `AR-7` out of it — an **Architecture Owner** act, not a freeze act. **Nothing is misconferred either way:** the rulings' Rank 5 authority comes from §4, which cites them by identifier (`AR-1`, `AR-3`, `AR-4`) and not by version | **P2** — documentation accuracy. Recorded as the **fourth drift** in `ADR-0053` §8.1, which is the section that would have read *\"search returned empty\"* had the search not been run |
| `D-10` demo surfaces present in code | **Release blocker** under `MP-CON-11` | **P0** — task `TASK-D10` |
| `IMPL-020` SMS provider / DLT registration not integrated | No OTP can be delivered. Blocks authentication **and** `IT-1` staff invitations | **P0** — on the critical path; DLT approval is multi-week and should be started first |
| ~~`tool/check_module_boundaries.dart` does not exist~~ | **CLOSED 2026-08-04.** Delivered as `IMPL-014`; **12 of 12** enforcement categories implemented per `ADR-0014`. `ADR-0001` is now mechanically enforced, not advisory | — |
| `app → domain/library` boundary findings deliberately **unwaived** | Gate 3 (`check_module_boundaries.dart`) **exits 1 by design** — 9 findings in 1 category. Not a checker defect and **must not** be silenced; clears when `TASK-D10` removes the demo surfaces (`ADR-0012` §3.4) | **P0** — tracked with `TASK-D10` |
| Library Management has **no** implementation code | Documentation is complete; `IMPL-100`…`IMPL-127` are unstarted | **P1** — 23 tasks, 12 at P0 within the module |
| `IMPL-220` — `SID-INT-*` integration rules unenforced | The twelve integration rules are specified but have no mechanical check; **unmet under `SID-4.56`** | P1 |
| ~~`R-5` — `lib/contracts/` referenced but absent~~ | **CLOSED.** Resolved as the pure-Dart package `packages/liboora_contracts`; law `L5` is now certified against real code rather than an empty directory | — |
| `D-8`, `D-9` | Carried forward unchanged | P2 |
| Public Live Occupancy (V2) | Deferred pending a privacy review; V1 exposes only a coarse indicator (`LIB-14B.12`) | P3 — **V2**, no V1 effect |
| Reviews & Ratings (V2) | No bounded context assigned; will require one | P3 — **V2**, no V1 effect |
| Development Standards document (`R-4`) | Deferred | P3 |
| `Q-02` — `branchId` model undecided | Multi-Branch is **V3**; no V1 effect, but the decision needs an ADR before Multi-Branch work starts | P3 — **V3** |
| `GCP-01` — this declaration drifted behind two Rank-4 documents | **CLOSED 2026-08-04 by correction.** §3.2 claimed BC Map v1.3 / Matrix v1.1 while the files read v1.4 / v1.2. Cause: both documents self-recorded a version bump without the §7 step 3 update here. **Prevention:** §7 step 3 already requires it — the rule was not weakened, it was not followed. A mechanical check comparing §3's versions against each file's header is recommended as a future gate | P2 — recurrence risk |
| `GCP-03` — this declaration contained a **broken changelog table** | **CLOSED 2026-08-04 by correction.** §8's `BASELINE-2026-08-04-B` and `BASELINE-2026-08-04` rows were joined into a single physical line by a literal `\n` (two characters, not a newline) left by the scripted insertion that wrote the new row. The table rendered as one malformed row, hiding the previous baseline's entry. Also corrected: §3.1's self-reference row still read `BASELINE-2026-08-04` after this document had been re-issued as `-B`, so the declaration disagreed with its own header. **Both found by item 8 validation, not by review** | P3 — cosmetic, but it concealed a changelog row |
| **`GCP-05`** — **`SM-GAP-10`: is Date of Birth mandatory at `BC-10`?** | **OPEN.** A tension **internal to `PRD-003` (Rank 3, frozen)**: §2.2 lists Date of Birth in the **Optional** field set and `SID-2.5` mandates only *"the minimum information required for identity"*, while **`SID-5.9` states it *"**SHALL** be stored, because minor-safety obligations (`ID-6`) depend on it."*** Predates `PRD-004`, which **discloses** it as `SM-GAP-10` and resolves it in neither direction. `PRD-004` is deterministic under both — `SM-INV-3` requires a guardian when age < 18 **or when minor-status is `Unknown`**, so the DOB-absent case is closed safely (`SM-AC-31`, `SM-AC-32`). **Resolution requires the `BC-10` owner and a separate ADR** amending `PRD-003` §2.2 or `SID-5.9`. Recorded by `ADR-0018` §3.1; originally finding `RF-17` | P2 — no V1 behavioural effect; a documentation contradiction inside a frozen Rank 3 document |
| **`GCP-06`** — **`SM-GAP-11`: is `BC-26` a consumer of `SM-EV-1` / `SM-EV-5`?** | **OPEN.** A tension **internal to the Bounded Context Map (Rank 4, frozen)**: **§8 names `BC-26` a primary consumer of `enrollment.StudentEnrolled` and `enrollment.StudentProfileUpdated`, while §7 declares no `BC-01`→`BC-26` edge**, and line 292 states an edge absent from §7 *"does not exist"* and that adding one requires an ADR. Predates `PRD-004`, which **discloses** it in §7.4, **omits `BC-26` from its consumer list and asserts no edge**; `SM-7.9` publishes facts without enumerating subscribers, so behaviour is identical either way. **Resolution requires the architecture owner and a separate ADR** — adding the §7 edge (the `ADR-0016` precedent) or removing `BC-26` from the two §8 rows. Recorded by `ADR-0018` §3.2; findings `SR-04`/`SR-05` | P2 — no V1 behavioural effect; §8 and §7 of the same frozen document disagree |
| No `.github/` issue or pull-request templates | Contributors get no structured prompt to cite an ADR or a requirement id | P3 |
| **`GCP-15`** — **this declaration's own header drifted two baselines behind its changelog** | **CLOSED 2026-08-19 by correction, in the same commit that admitted `PRD-013`.** The header declared **`BASELINE-2026-08-05-A`** while §8's latest row read **`BASELINE-2026-08-17-A`** (`ADR-0049`, the `PRD-008` freeze). The `PRD-008` admission updated §8 and §3.3 and **not** the header, so the document's most prominent field was the wrong one. **No status was ever misconferred** — `PRD-008`'s Rank 3 authority came from its §3.3 row and the `ADR-0049` acceptance, both correct throughout. Same defect class as `GCP-01`/`07`/`08`/`11`: **a derived statement left behind by a change to the thing it describes** | — |
| **`GCP-16`** — **§4 drifted behind fifteen accepted ADRs and one Rank 3 freeze** | **CLOSED 2026-08-19 by correction, in the same commit that admitted `PRD-013`.** The Rank 2 row read *"`ADR-0001` … `ADR-0020`, `ADR-0032`, `ADR-0033`, `ADR-0034`"* after `ADR-0035`…`0037`, `0039`, `0040` and `0042`…`0049` were accepted; and the Rank 3 list held **seven** module baselines with prose saying *"seven"*, omitting **`PRD-008` v1.7** though §3.3 already carried its row. Rank 2 is now stated as a **measured count (36 accepted of 50 files)** with the 13 `Proposed` ADRs named as non-binding, so the row can be re-derived rather than incremented. The original *"seven"* sentence is **preserved verbatim** beneath the correction | — |
| **`GCP-17`** — **`PRD_REGISTRY.md` §7 counted a frozen PRD as `DRAFT`** | **CLOSED 2026-08-19 by correction, in the same commit that admitted `PRD-013`.** §7 listed `PRD-008` under **`DRAFT`** with `FROZEN` at **7**, though `ADR-0049` conferred its Stage 7 freeze on 2026-08-17. The registry's §4 table and this declaration's §3.3 were both correct; only §7's summary counts were stale. Third instance of this class found in one admission, which is why all three are logged rather than absorbed — **seven instances across four documents is an argument for a mechanical check, not for more diligence** | — |
| **`GCP-18`** — **`PRD_REGISTRY.md` §7's document-existence enumeration never recorded `PRD-013`** | **CLOSED 2026-08-19 by correction, in the same commit that admitted `PRD-016`.** §7 *Documents that exist* read *"13 files, **11** PRD identities — `PRD-000`…`PRD-008`, `PRD-019` and `PRD-022`"*, so the register simultaneously stated that `PRD-013` was **`FROZEN`** (adjacent row, correct) and that **no `PRD-013` document existed**. Measured against disk: **13** identities carry a specification file, and the enumeration omitted **`PRD-013`** and **`PRD-016`**. The `ADR-0050` commit advanced this row's `FROZEN` and `PLANNED` counts but not its existence list. **It never misconferred a status** — `PRD-013`'s freeze rests on its §3.3 row, which was present and correct. ⚠ **Found by testing a claim, not by reading:** `ADR-0051` §8 first asserted *"no pre-existing defect was found"*, and measuring that assertion falsified it — recorded in `ADR-0051` §8.1 rather than quietly corrected. ⚠ **A disclosed limit on the repair:** the row states **no rule** for what counts as one *file* (`PRD-002` alone has four), so the **identity** count is re-derived by measurement while the **file** count is advanced by **increment** and labelled as such. **Eighth instance of this class, across five documents** | P2 — recurrence risk |
| **`GCP-19`** — **§4's Rank 2 count sat one behind its own enumeration** | **CLOSED 2026-08-19 by correction, in the same commit that admitted `PRD-016`.** The Rank 2 row read *"**36 accepted** of 50 files"* while the very same cell enumerated *"`ADR-0042`…**`ADR-0050`**"* — measured, there were **37 accepted of 50** before `ADR-0051`. `ADR-0050` was added to the enumeration without the leading number advancing, which is **`GCP-16`'s defect recurring in the row `GCP-16` had just repaired**. The correct figure was recovered by classifying all 51 files' own `Status` rows on the leading declared token rather than by incrementing: **51 / 38 / 13 / 0**. **No ADR's binding force was ever affected** — an accepted ADR binds by its own `Status`, not by this count. **The enumeration was right and the number was wrong**, the same direction of error `ADR-INDEX.md` has recorded of itself several times, and the row now names the instrument it was derived with. **Ninth instance of this class** | P2 — recurrence risk; the strongest argument yet for a mechanical count gate |
| **`GCP-07`** — **§4 and Rank 2 drifted behind two accepted freezes** | **CLOSED 2026-08-04 by correction, in the same commit that admitted `PRD-007`.** §4 listed **four** Rank 3 module baselines and the prose said *"four"*, but `PRD-005` had been admitted at Rank 3 by `ADR-0019` under `BASELINE-2026-08-04-D` and never received a §4 row; the Rank 2 row still read *"`ADR-0001` … `ADR-0017`"* though `ADR-0018` and `ADR-0019` were accepted. **Cause: the `-D` declaration updated §3.3 and §8 but not §4** — the same §7 step 3 failure recorded as `GCP-01`, in a different section. **This is disclosed rather than absorbed:** the `PRD-007` freeze would otherwise have appeared to add one row when it in fact corrected two omissions and added its own. `PRD-005`'s Rank 3 authority was never in doubt — §3.3 and the `ADR-0019` acceptance both conferred it; only this table was incomplete. **Prevention:** a mechanical check that every §3.3 row marked *Authoritative* has a matching §4 row is recommended, alongside the `GCP-01` version check | P2 — recurrence risk; second instance of the same §7 step 3 failure |
| **`GCP-08`** — **`PRD_REGISTRY.md` §4.1 prose contradicted its own table for three freezes** | **CLOSED 2026-08-04 by correction, in the same commit that admitted `PRD-007`.** The paragraph introducing §4.1 read *"**None was ever written.** All nine are `PLANNED`"* while the table directly beneath it recorded `PRD-004` and `PRD-005` as **`FROZEN`**. True when written; false from the moment `ADR-0018` was accepted, and it survived the `PRD-004` and `PRD-005` freezes because **a status change updates the row and nobody re-reads the sentence above it**. **No status was ever misconferred** — the table, which §2.1 makes the operative record, was correct throughout; only the derived prose was stale, and per `PRD_REGISTRY.md` §Precedence a register defect never overrides a PRD. **Disclosed rather than absorbed** because it is the same class of defect as `GCP-01` and `GCP-07`: a *derived* statement left behind by a change to the thing it describes — three instances now, in three different documents, which is the argument for a mechanical check rather than more diligence. **Prevention:** a check that no governance document asserts a count or a universal in prose that its own adjacent table contradicts; the three instances share one cause and would share one gate | P2 — recurrence risk; third instance of derived-statement drift |
| **`GCP-09`** — **`PRD-007` citation columns point at renumbered rules — 10 confirmed instances** | **OPEN.** Found during Stage 7 developer-documentation authoring, **after** the gates passed and disclosed rather than fixed. Ten *Authority* / parenthetical citations name a rule that exists but governs something else, because `SEAT-BR-018`, `SEAT-BR-020` and `SEAT-BR-022` were renumbered during a Stage 4 correction pass: the prose definitions moved, the pointer columns did not. Confirmed: **§8 gate table** — G11 *Not in maintenance* cites `SEAT-BR-020` (which is the attendance non-ownership rule; intended `SEAT-FR-043`/`SEAT-BR-026`), G12 *Not locked* cites `SEAT-BR-018` (booking mode; intended `SEAT-BR-025`), G15 *No conflicting reservation* cites `SEAT-BR-011` (one allocation per seat; intended `SEAT-INV-004`/`SEAT-FR-042`); **§6.4 invalid-transition table** — three rows (L460–L462) with the same two substitutions; and four further parentheticals (L432, L448, L450, L1029) citing `SEAT-BR-020`/`SEAT-BR-022` where `SEAT-BR-026` is meant. **Six further candidates were examined and rejected**: the same three identifiers are cited *correctly* elsewhere (`SEAT-AC-212` for booking mode, the attendance rows, the flexible-seat rows), which is what makes the drift hard to see. **No behavioural ambiguity and no requirement is wrong.** Each gate's required behaviour is stated in its own *Gate*/*Failure reason* columns and restated normatively in the owning chapter, so a developer following a bad pointer lands on a real but irrelevant rule — confusion, not divergent implementation. **Why five gates missed it:** Stage 5 verifies every cited identifier **exists** (0 dangling — all ten do exist) and Stage 6 verifies every obligation is **claimed** (443/443); **neither compares a citation's meaning to the cited rule's text, and no script in this repository does.** It is a semantic defect inside a mechanically clean document. **Not fixed, on the explicit authority of `PRD_LIFECYCLE.md` Stage 7** — *"A frozen PRD is never silently modified. Not for an obvious correction, and not for one that is certainly right"* — the same rule that leaves `PGA-01` and `PGA-02` standing. **Resolution:** one clarifying-wording pass under baseline §7 (changelog entry, no ADR, no version increment, no business-rule change), owned by the `BC-04` specification owner. **Prevention:** a gate asserting that a rule cited as authority for a topic shares vocabulary with its own definition — the first check here that would need to read meaning rather than count identifiers | P2 — navigational; misleads a reader of the most-read table in the PRD, changes no behaviour |
| **`GCP-10`** — **two navigational documents described an unwaived boundary edge as waived** | **CLOSED** in the same commit that found it. `seat-management/README.md` and `PRD-007_DEVELOPER_GUIDE.md` §6 both stated that the nine `app → domain/library` boundary findings were *"waived to 2026-10-31 by `ADR-0012`"*. They are **not waived**: `ADR-0012` §3.4 deliberately withholds the waiver because the sites are already tracked by `TASK-D10` (`BLK-01`), a P0 release blocker, and §4's removal table records the edge as *"Not waived. Closes with `TASK-D10`"* with expiry **n/a**. 2026-10-31 is the expiry of two unrelated Wave-1 exceptions, which is how the two were conflated. **The direction of both warnings was already right** — gate 3 is red, it is pre-existing, do not add to it — so no reader was told the gate was green or that the edge might be imported; only the mechanism was misstated. Corrected to state four facts: not waived · an intentional tracked release blocker · red until `TASK-D10` completes · pre-existing and unrelated to `PRD-007`. Both now also record that a waiver would turn **gate 4** red, since `boundary_checker_test.dart` asserts the edge's absence from the exception list. **No ADR required**: both documents are unranked (§3.3 *"navigational, not part of the specification"*; §3.4 *"carries no requirement and no authority"*) and §7 step 1 scopes the ADR requirement to Rank 1–5. Established mechanically before the edit: the checker's output is byte-identical at `7602890` and `a8037da` (SHA256 `66d14bbe…`), and `git blame` places all nine lines in scaffold commit `a44ebb0e`. **Neither `ADR-0012`, `ADR-0014`, `TASK-D10`, `module_dependencies.yaml`, the boundary checker, nor `PRD-SEAT-MANAGEMENT.md` was modified** — the PRD remains byte-identical at `c8760a46…`. Fourth instance of the `GCP-01`/`GCP-07`/`GCP-08` class, and the first found in a document *this* work created rather than inherited. | **Closed** |
| **`GCP-11`** — **three derived statements in this declaration drifted behind the documents they describe** | **CLOSED 2026-08-05 by correction, in the same commit that admitted `PRD-006`.** Three cells were stale at once: §3.1's self-reference read `BASELINE-2026-08-04-D` while the header row read `BASELINE-2026-08-04-E`; §3.2 recorded `LIBOORA_BOUNDED_CONTEXT_MAP.md` at **v1.5**, having missed the v1.6 bump made by `ADR-0032` *and* the v1.7 bump made by `ADR-0033`; and the Rank 2 rows in §3.1 and §4 still read *"`ADR-0001` … `ADR-0020`"* after `ADR-0032` had been accepted. **None of the three ever misconferred a status** — no document was treated as ranked, frozen or binding when it was not — but a declaration that does not match the repository is, by this file's own §7 step 3, *"worse than no baseline"*. **This is the fourth recorded occurrence of one defect class**, after `GCP-01` (§3.2 behind two Rank 4 documents), `GCP-07` (§4 and Rank 2 behind two freezes) and `GCP-08` (registry prose contradicting its own table): **a derived statement left behind by a change to the thing it describes.** The recurrence is the finding. Three independent authors each followed §7 steps 1, 2 and 4 correctly and each missed step 3 on a *different* cell, which is evidence that the control is **unenforceable by diligence**: every one of these is mechanically checkable — compare the header identifier against the §3.1 cell, compare each §3.2 version against the target file's own version row, and compare the Rank 2 range against the highest-numbered `Accepted` ADR on disk. **Prevention is therefore recorded as a gate that does not yet exist rather than as a promise to be more careful**, and it is deliberately **not** written in this commit: adding a checker while freezing a PRD would mix two changes under one authority. **Recorded, owned, and open as a tooling task** | **P1** — documentation-integrity checker; **the defect itself is closed, the absence of a check for it is not** |
| **`GCP-12`** — **three statements I wrote while freezing `PRD-006` failed their own re-measurement** | **CLOSED 2026-08-05 by correction, in the freeze commit itself.** Not inherited drift — **self-inflicted, caught by measuring prose I had just authored.** *(1)* `PRD-006`'s new freeze changelog row claimed *“the total line count is unchanged at 3215”*; the file is **3216**, because **that changelog row is itself the added line** — a claim that could never have been true, and one the load-bearing body-md5 evidence never needed. *(2)* §3.3 of this document asserted the Seat Card has *“exactly four states — NOT ATTENDED, PRESENT, ATTENDED·PRESENCE UNVERIFIED, ATTENDED·LEFT”*, and *(3)* `ADR-0034` repeated the same enumeration with emoji. **Measured against the sources, that enumeration is unsourced:** the frozen `PRD-006` contains those four labels **zero times**; its §23.4a lists **six** requested renderings; the four-label vocabulary is [`ADR-0032`](adr/ADR-0032-attendance-presence-visibility-to-seating.md) **L302**, whose **L304 records `ATTENDED · LEFT` as still BLOCKED** because `SEAT-BR-036` puts period aggregation in `BC-26` and `SEAT-FR-114` resets occupancy daily. Both derived statements had therefore **promoted a blocked rendering to a delivered one** and fixed a count their source does not fix. **Root cause:** a summarised decision list was trusted as a citation instead of being read back against the document it summarised — the **fifth** recorded occurrence of the `GCP-01`/`07`/`08`/`11` class, and the first where the drifting author was the one writing the freeze. **Fixed by withdrawing each claim in place, quoting the false wording beside its correction**, so a reader sees what was wrong rather than a clean surface. **No requirement, rule, event, criterion or gate was touched:** `PRD-006`'s body md5 is still `8fb479c2387d1098a47824eb8b9b69a9`, and what the baseline actually admits about the Seat Card — read-time composition, never reading raw Wi-Fi or device state, manual attendance ≠ verified presence, seat allocation ≠ presence — is **unchanged and was never in doubt**. **Prevention, stated honestly:** the mechanical check `GCP-11` called for still does not exist, and this row is a second data point that a *derived-statement* checker — one that re-reads every count and enumeration a governance file asserts about another file — is the missing control. It is **deliberately not written in this commit**, because adding a tool while freezing would put an unreviewed gate into the same commit as the freeze it polices. |
| **`GCP-13`** — **a task count I fabricated propagated into five governance documents** | **CLOSED 2026-08-05 by correction across all 11 occurrences.** While writing the freeze records I stated `PRD-006` has **179** implementation tasks, and repeated it as *“0 of 179 tasks … are proven”* in `ADR-INDEX.md` (×3), this declaration (×2), `ADR-0034` (×2), [`PRD_REGISTRY.md`](prd-ecosystem/PRD_REGISTRY.md) (×1) and `PRD-006` itself (×3). **The gate contradicts it.** `prd006_task_coverage.py` — the Stage 6 check, reading [`PRD-006_IMPLEMENTATION_TASKS.md`](../40-implementation/attendance-management/PRD-006_IMPLEMENTATION_TASKS.md) at its own L104 — prints **`Tasks parsed: 80`** and **`Allocation span: IMPL-600..679 / 0 holes`**. Measured at three commits (`01684e5`, `fce7848`, `bdebd1e`) the task document has had **80 rows throughout**; there was never a 179-task version. **Root cause:** `179` is `285 − 106` — an arithmetic coincidence with the obligation register, written from memory of a *different* number and never read back against the tool that produces it. **The `285/285 = 100.0%` figure beside it was always correct**, which is precisely why the wrong number survived review: it sat next to a right one. **Fixed** by replacing all 11 occurrences with the gate-measured **80**; `PRD-006`'s two occurrences are at L10 and L3197, both **outside** the frozen body range, so the body md5 `8fb479c2387d1098a47824eb8b9b69a9` is unchanged and was re-measured after the edit. **This is the same defect class as `GCP-12`, one commit later, and the second instance where the drifting author was the one writing the freeze** — which is the strongest evidence yet that the derived-statement checker named in `GCP-11` and `GCP-12` is a genuinely missing control and not a nice-to-have. It remains **deliberately unwritten** here for the reason `GCP-12` gives. **Nothing else changed:** no requirement, rule, event, criterion, ADR status or gate was touched, and no gate result moved. |

**Two gaps left this table on 2026-08-03**, and the distinction matters:

- *"Library PRD §§1–25 never supplied"* — **closed.** The specification exists. See §2.1.
- *"Invitation security specification never written"* — **closed.** The specification exists. See §2.1.

Nothing else moved. In particular `D-10` and `IMPL-020` are **code** gaps, and a document cannot close a code gap
however carefully it is written. They are restated here as tasks, not as findings awaiting a decision.

---

## 7. Changing the baseline

1. A change to any Rank 1–5 document requires an ADR **before** the change.
2. The changed document's version is incremented and its changelog updated in the same commit.
3. This declaration is updated in the same commit. A baseline that does not match the repository is worse than no
   baseline.
4. The baseline identifier changes only when a Rank 1–3 document changes version.

---

## 8. Changelog

| Baseline | Date | Change |
|---|---|---|
| **BASELINE-2026-08-20-B** | 2026-08-20 | **`PRD-017` File & Media admitted to the baseline — one ADR accepted, zero ranked documents amended in content, zero requirements changed, and the pre-existing-defect search returned SIX drifts rather than none.** Issued because a **Rank 3** document entered the baseline (§7 rule 4): [`ADR-0054`](adr/ADR-0054-file-media-prd-v0.1-baseline.md) admits `30-product/file-media/PRD-017_FILE_AND_MEDIA.md` **v0.1** at Rank 3 for **`BC-29` File & Media** — the **thirteenth** module baseline in §3.3, and **the first of the two `Platform Services` contexts to gain a Rank 3 specification** (BC Map §3.3's subdomain column pairs `BC-29` with `BC-30` Offline Sync, which is `PLANNED`). ⚠ **This is the second suffix advance within a single day**, after `-A`; the only prior same-day pair is `BASELINE-2026-08-19-A` → `-B`. ⚠ **Neither adjacent predecessor's band claim was reused, because both were tested and both are false here:** `PRD-016` claimed the *"first CAPABILITY & GENERIC band context frozen"* — that band (BC Map **L258–268**) holds **eleven** contexts and `BC-21`/`BC-24` are already frozen, making `BC-29` **3 of 11**, an unremarkable figure published because it is the true one; `PRD-023` claimed a FOUNDATIONAL *"3 of 3"*, and `BC-29` is not FOUNDATIONAL. Admitted at **v0.1**, the version verified — **not** renumbered, per `ADR-0020` §4 item 2; the **fifth** v0.x admission after `PRD-013`, `PRD-016`, `PRD-014` and `PRD-023`. Figures recomputed by **two independent checkers**, neither importing the other, reaching the same totals by different parsing strategies: **232** identifiers / **8** registers / **78** AC / **132** Class A / **94** covered = **71.2%** / **40** tasks `IMPL-1200`…`1239` claiming **132 of 132 = 100.0%**, with `IMPL-1240`…`1299` reserved; **479** citations resolved across **24** scopes, **0** duplicates, **0** dangling, **12/12** mutants killed. ⛔ **Student-to-Student Text + File/Media Sharing is RETAINED in V1 on instruction, and the freeze is honest that it cannot yet be served.** BC Map **L331** lists `E-22`'s consumers as `BC-01`, `BC-10`, `BC-14` and **not `BC-12` Messaging**. Stage 3 tested necessity **per context** rather than granting broadly — **`BC-12` YES** (the minimal sufficient consumer), **`BC-11` NO**, **`BC-13` NO** (`E-14` is outbound) — recorded as accepted finding `S3-A-01`. ⚠ **The central decision of this pass was a refusal.** `ADR-0016` is the exact one-cell precedent for widening `E-22`, it was identified precisely, and `ADR-0054` §3.2 **declines to exercise it**: BC Map amendment belongs to the **Architecture Owner** and the authority conferred here is **Governance Owner** — *"knowing the right amendment is not the same as holding the right to make it."* The gap is **routed to a named owner as `FIL-GAP-012`/`B-11`, not resolved**, on the **measured** `ATT-GAP-010` precedent (`PRD-006` **L1564**/**L2973**) that a freeze may carry an open `E-22` consumer gap transparently. **Five refused alternatives are tabulated in §3.3**, including the subtlest — routing sharing through **`BC-14` Content Sharing**, which *is* already an `E-22` consumer and would have made the gap vanish: **a message attachment is not a content post**, and the move is recorded as *"architecturally clever and factually wrong"*. **`ADR-0022`, which frames the same `E-22` question for `BC-03`, remains `Proposed` and was NOT promoted to settle this by convenience.** ⚠ **`FIL-CFG-*` is POPULATED at nine — the first v0.x admission whose CFG register is not declared empty.** All four predecessors refused to mint configurables for want of a bounding authority; here size limits, retention windows, MIME allow-lists and signed-URL TTLs **are** bounded by readable authorities, so nine members were derived rather than invented **and rather than reflexively refused**. **`FIL-EVT-*` is admitted EMPTY as a finding** — `BC-29` is a producer in **zero** BC Map §9 rows, so an event name would create an edge **L292** says does not exist; the **fifth** declared-empty EVT register. ⚠ **Coverage is admitted at 71.2% and is NOT conflated with the 100.0% task coverage** — different claims; the **38** uncovered obligations are enumerated by class (**XC 17 · FR 17 · BR 3 · INV 1**) with **0 undisclosed**, and the XC majority is structural: a criterion asserting that something never happens is unfalsifiable by observation. **0 aggregates asserted beyond the BC Map grant, 0 events, 0 `E-*` edges, 0 BC Map cells, 0 ports, 0 requirements moved.** No `MASTER_PRD.md`, BC Map, Dependency Matrix, `ARCHITECTURE_RULINGS.md`, `CONFIGURATION_GUIDE.md`, `tool/module_dependencies.yaml` or pre-existing `tool/docs_check` change. **No code** — `git status --short lib/ packages/ test/ web/ pubspec.yaml` → **0 lines**; `check_module_boundaries.dart` output **byte-identical** to baseline, so **0 new** boundary findings. ⛔ **`FROZEN` is not `VERIFIED`: 0 of 78** criteria and **0 of 40** tasks proven by a test; **7 of 7** required architecture tests missing (`B-2`); **all 13 `FIL-GAP-*` OPEN**; **`IMPL-1230`…`1236` BLOCKED** by `B-11`. ⚠ **§8.1 of `ADR-0054` records that the pre-existing-defect search was performed and did NOT return empty** — the discipline `ADR-0051` §8.1 established after its own no-defect claim was falsified. **Six drifts:** **drift 1**, the decisive one — **three of the four hashes cited by PRD-017's gate records were STALE**, and the convenient `PRD-016`/`PRD-023` precedent sentence (*"all five gate records measured the identical hash … no supplement was required"*) was **tested and falsified rather than inherited**; the full eight-state hash lineage was reconstructed from git, the drift measured as **`+2 FIL-AC-*` and nothing else** with every Class A register identical in membership, and a **Stage 3 supplement was written**, with the general instrument defect opened as **`GCP-22`**; **drift 2**, `S4-D-06` absent from the Stage 4 record that should carry it — recorded in `ADR-0054` §3.1 rather than back-dated; **drift 3**, three instrument defects including `S5-C-07`'s directory-vs-line-kind scoping error, whose repair moved resolved citations **141 → 479** and exposed **3** genuine dangling references the narrower scope had never looked at; **drift 4**, six phantom citations the author introduced into the conferral record, found by an out-of-band grep and then **reintroduced while documenting them** — the checker was right both times and **the prose was fixed, not the rule**; **drift 5**, two stale Rank 1 / Rank 4 statements left unrepaired — `PRD_LIFECYCLE.md` **L147–153**'s `IMPL-227+` table, which would have collided with **four** reserved ranges had it been obeyed, and `PRD_DEPENDENCY_GRAPH.md` **L113**'s *"PRD-002 owns BC-29"*; **drift 6**, **`GCP-20` preserved rather than silently corrected**, with the consequence stated: §4 now holds **12** Rank 3 rows against §3.3's **13**, so the arithmetic gap **survives this admission by design**. ⚠ **A seventh finding was made and repaired in this edit rather than carried**: §4's *"Rank 3 holds eleven module baselines"* sentence was **stale by one** — `ADR-0053` §11 **L323** already states *"twelve"* — so it advanced **by two, to thirteen**, the recurrence recorded rather than smoothed. Rank 2 extends to **`ADR-0054`** (**54 files / 41 accepted / 13 proposed / 0 superseded / 0 deprecated**, re-derived by classifying **all 54** files' own `Status` rows; each sum equals the file count and **only the accepted count moves**) |
| **BASELINE-2026-08-20-A** | 2026-08-20 | **`PRD-023` Settings & Configuration admitted to the baseline — one ADR accepted, zero ranked documents amended in content, zero requirements changed, and the pre-existing-defect search returned FOUR drifts rather than none.** Issued because a **Rank 3** document entered the baseline (§7 rule 4): [`ADR-0053`](adr/ADR-0053-configuration-prd-v0.1-baseline.md) admits `30-product/configuration/PRD-023_SETTINGS_AND_CONFIGURATION.md` **v0.1** at Rank 3 for **`BC-25` Configuration** — the **twelfth** module baseline in §3.3, and **the third and last `FOUNDATIONAL`-band context to gain a Rank 3 specification**: BC Map **L271** places `BC-18`, `BC-19` and `BC-25` in that band, `BC-18` is held by the Authentication PRD and `BC-19` by `PRD-013`, so **the band is now 3 of 3 covered** — measured, not inferred from the adjacent precedent phrase. Admitted at **v0.1**, the version verified — **not** renumbered, per `ADR-0020` §4 item 2; the **fourth** v0.x admission after `PRD-013`, `PRD-016` and `PRD-014`. Figures recomputed by **three instruments, none importing another, using three different parsing strategies** (line form, scope position, task-document parsing with an independently re-derived obligation set): **180** identifiers / **8** registers / **59** AC / **113** Class A / **76** covered = **67.3%** / **30** tasks `IMPL-1100`…`1129` claiming **113 of 113 = 100.0%**, with `IMPL-1130`…`1199` reserved. **All five gate records measured the identical hash `e974960…6399326`** — verified **five** times — so **no alignment supplement was required and none was written**, as with `PRD-016` and unlike `PRD-008`/`PRD-013`. ⚠ **Coverage is admitted at 67.3% and is NOT conflated with the 100.0% task coverage** — they are different claims, and the reason the first is lower is structural rather than a shortfall in diligence: **all 16 `CNF-XC-*` are uncovered by construction**, because an exclusion states what must be *impossible* and a criterion asserting that something never happens is unfalsifiable by observation. The precedent avoided is named in the PRD's own Stage 6 record — `PRD-006` v1.0 published *"100% coverage"* against a true **49.1%**. ⚠ **Two registers admitted EMPTY rather than populated, as findings** (`CNF-EVT-*`, `CNF-CFG-*`): `BC-25` is a producer in **zero** BC Map §9 rows, so an event name would create an edge **L292** says does not exist; and `ADR-0017` §3.1 item 6 gives this module *"the resolution machinery, **not** the value list"*, so a numbered `CNF-CFG` member would have duplicated one of the **104** configurables already owned by eight frozen PRDs or invented a 105th. **Emptiness is enforced as a test in three checkers**, not merely asserted — the fourth declared-empty CFG register. ⚠ **Stage 3's verdict is UNCONDITIONAL**, stronger than `PRD-013`'s and `PRD-014`'s, for one measured reason: `BC-25`'s FOUNDATIONAL band means the inherited **`H-1`** taxonomy gap does not apply. **0 aggregates asserted** — `CNF-GAP-001` is a live Rank 3 vs Rank 4 conflict and **the PRD takes neither side**. **0 events, 0 `E-*` edges, 0 configurables, 0 requirements moved**; `ADR-0017` §3.2's *"This ADR moves no requirement"* held. **No `MASTER_PRD.md`, BC Map, Dependency Matrix, `ARCHITECTURE_RULINGS.md`, `CONFIGURATION_GUIDE.md`, `tool/module_dependencies.yaml` or pre-existing `tool/docs_check` change. No code** — `git status --short lib/ packages/ test/ web/ pubspec.yaml` → **0 lines**. ⛔ **`FROZEN` is not `VERIFIED`: 0 of 59** criteria and **0 of 30** tasks proven by a test; **all 8 `CNF-GAP-*` OPEN**; **9 blockers, 5 ⛔ hard-external**. ⚠ **`S4-D-01` disclosed, not repaired** — the PRD over-declares its uncovered set by three, which **understates** coverage, and repairing it would change the hash four gate records are anchored to. ⚠ **Three self-inflicted instrument defects recorded rather than hidden** (`S5-C-01`…`03`), plus **four dangling citations the author introduced while describing the very mutations that prove the gate catches phantoms** — the checker was right and **the prose was fixed, not the rule**; a carried `M11` miss **re-run and proven a harness defect rather than a gate blind spot**, re-tested as M11a/M11b (**12/12**); and a regression sweep that reported **20/20 clean while contradicting 5 known failures**, root-caused to a `case … continue` resetting `$?` (`S5-F-02`) — after correction **exactly the 5 known pre-existing failures reappeared and nothing had been weakened**. **19 mutations injected across Stages 5 and 6, all 19 caught**, every document restored byte-identical. ⚠ **§8.1 of `ADR-0053` records that the pre-existing-defect search was performed and did NOT return empty** — the discipline `ADR-0051` §8.1 established after its own no-defect claim was falsified. **Four drifts:** **`GCP-20`** (§4 missing the `PRD-014` Rank 3 row) **carried unrepaired on explicit instruction**, with the consequence stated that §4 now holds 11 rows against §3.3's 12; **drift 2**, §4's Rank 2 row reading *"38 accepted of 51 files"* against a measured 52/39 — `GCP-16`/`GCP-19` recurring a **third** time in the one cell they were opened to repair — **repaired** because this admission must extend that cell; **drift 3**, §3.1's self-reference **three** baselines stale, which is **`GCP-15` recurring after being recorded CLOSED**, **repaired** under §7 rule 3 and logged as a recurrence; and **`GCP-21`**, §3.2 recording `ARCHITECTURE_RULINGS.md` at v1.2 against the file's own v1.1, **left open rather than settled by guessing which record is wrong**. Rank 2 extends to **`ADR-0053`** (**53 files / 40 accepted / 13 proposed**, re-derived by classifying every file's own `Status` row; **only the accepted count moves**) |
| **BASELINE-2026-08-19-C** | 2026-08-19 | **`PRD-014` Entitlement admitted to the baseline — one ADR accepted, zero ranked documents amended in content, zero requirements changed, and zero pre-existing governance defects found.** Issued because a **Rank 3** document entered the baseline (§7 rule 4). Admitted at **v0.1**, the version verified — **not** renumbered, per `ADR-0020` §4 item 2; the **third** v0.x admission after `PRD-013` and `PRD-016`. Figures recomputed by **three instruments using three different parsing strategies** (line form, scope position, table-column semantics), never copied from the stage reports: **88** identifiers / **8** registers / **26** AC / **54** Class A / **51** covered = **94.4%** / **30** tasks `IMPL-1000`…`1029` claiming **54 of 54 = 100.0%**. The `IMPL` range was established by enumerating **489** distinct real identifiers, because `PRD_LIFECYCLE.md`'s own table is **stale** (`IMPL-227+ | Unallocated` while `IMPL-227` is occupied) — **disclosed for the fourth consecutive backlog and left intact**, since repairing a Rank 1 table requires its own ADR. The **5** pre-existing `IMPL-1000` hits were opened and shown to be *"Unallocated"* **boundary labels**: occurrence is not occupancy, and they are disclosed rather than filtered so a reader re-running the command can reconcile them. ⚠ **Two registers admitted EMPTY rather than populated** (`ENT-EVT-*`, `ENT-CFG-*`) — `BC-21` publishes nothing and no authority bounds any entitlement parameter, so **a timeout, grace period and fail disposition were each refused rather than invented** with `Q-03`/`Q-06` OPEN. ⚠ **Stage 3's CONDITIONAL verdict is recorded as CONDITIONAL**, its condition being the **inherited** `H-1`. ⛔ **0 of 26** criteria proven, **0 of 30** tasks implemented, **8 `ENT-GAP-*` OPEN**, **`D-014-01`…`06` NOT fixed** — `git status --short lib/ packages/ test/` → **0 lines**. ⚠ **Nine self-inflicted instrument defects recorded, not hidden** (`S5-C-01`…`06`, `S6-C-01`…`04` less overlap), including two caught by a guard catching its own author's bad input and one where **the checker was wrong and the document right**; **13 mutations injected, 13 caught**, all documents restored byte-identical. ⚠ **§8.1 of `ADR-0052` records that the pre-existing-defect search was performed and returned empty** — following `ADR-0051` §8.1, which obliges the test rather than the conclusion after its own claim to have found nothing was falsified. Matrix **§2J.1's false `FR-*` location claim** (`S5-F-01`) disclosed, **not edited** |
| **BASELINE-2026-08-19-B** | 2026-08-19 | **`PRD-016` Audit Trail admitted to the baseline — one ADR accepted, zero ranked documents amended in content, zero requirements changed, and two pre-existing governance defects repaired.** Issued because a **Rank 3** document entered the baseline (§7 step 4): [`ADR-0051`](./adr/ADR-0051-audit-trail-prd-v0.1-baseline.md) admits `30-product/audit/PRD-016_AUDIT_TRAIL.md` **v0.1** at Rank 3 as the authoritative specification for **`BC-24` Audit Trail** — the **tenth** module baseline, the **tenth** Rank 3 row, and the **first context in the CAPABILITY & GENERIC band** to hold a frozen product specification (measured against all nine prior freezes: none of that band's eleven contexts was owned by any of them). **This is the first baseline identifier to advance by suffix within a single day** — `-A` → `-B` on 2026-08-19 — which is the mechanism §7 step 4 prescribes when a Rank 1–3 document changes version and the date does not move. **Five validation records admitted with it**, and **no alignment supplement was written** because none was needed: all five gate records measured the identical hash `281f15e8…f339be`, so the staleness that forced supplements for `PRD-008` and `PRD-013` does not arise here. Its absence is stated explicitly, because a missing supplement is otherwise indistinguishable from an omission. ⚠ **The second v0.x document admitted, and deliberately so:** `ADR-0020` §4 item 2 admits *"each at the version that was verified"* and §5 rejects renumbering at freeze, so the version is **preserved at v0.1**; `ADR-0050` §5 had already **withdrawn as an invented rule** the contrary claim that a v0.x document must be released to a version before freeze, and reviving it here would have re-introduced a prohibition the repository has rejected in writing. `PRD-016`'s only edits are its freeze metadata, its §12 lifecycle table (five rows that were **false** against conferred verdicts, corrected with every superseded value quoted) and one changelog entry. **No identifier added, removed, renumbered or reworded** — 58 declared, 0 retired. ⚠ **Two registers are admitted DELIBERATELY EMPTY, and this is a finding rather than an omission.** `AUD-EVT-*` holds nothing because `BC-24` is a producer in **zero** BC Map §9 rows and a consumer in **25** — audit is a terminal sink, and inventing an event name would have created an edge BC Map L292 says does not exist. `AUD-CFG-*` holds nothing because **no Rank 1–7 authority supplies a retention period, legal-hold trigger or bound**: `MP-NFR-10` assigns retention and legal hold to SECURITY + DATA Governance, `Q-04` records the period as *"Open — needs counsel"*, and `CONFIGURATION_GUIDE.md` has no audit row. **A retention period invented here would have been a legal determination made by a document with no standing to make one**, so it is carried as `AUD-GAP-001` instead. **No `MASTER_PRD.md`, BC Map, Dependency Matrix, `ARCHITECTURE_RULINGS.md`, `CONFIGURATION_GUIDE.md`, `tool/module_dependencies.yaml` or `tool/docs_check` change. No code.** **Five `AUD-GAP-*` admitted OPEN**, including the `AUTH-10.27` / `E-20` tension (`AUD-GAP-002`), which is **left undecided on purpose** — settling it would amend one of two **frozen** documents without an ADR, and the backlog is instead written so the module's obligation is identical under either reading. Three **code** defects carried, not fixed — `D-016-01` (`recent()`/`count` read with no tenant argument, an `X-13` exposure), `D-016-02` (a missing audit timestamp silently becoming **1970-01-01** rather than failing loudly), `D-016-03` — routed to `IMPL-922`, `IMPL-910`…`912` and `IMPL-913`. ⚠ **`FROZEN` is not `VERIFIED`: 0 of 30 tasks, 0 of 18 acceptance criteria proven by a test**, and coverage is admitted at its measured **77.1%** with the **8** uncovered obligations **named** rather than rounded up. Rank 2 extends to **`ADR-0051`** (**38 accepted · 13 proposed · 51 files**, re-derived by classifying every file's own `Status` row). Defects repaired and logged: **`GCP-18`** (the registry's document-existence enumeration never recorded `PRD-013`, found by **testing** `ADR-0051` §8's own claim to have found nothing, which falsified it) and **`GCP-19`** (this §4's Rank 2 count one behind its own enumeration — `GCP-16`'s defect recurring in the row `GCP-16` had just repaired). **Nine instances of the derived-statement class now stand across five documents**, and two of the last three were found by an instrument rather than by reading. |
| **BASELINE-2026-08-19-A** | 2026-08-19 | **`PRD-013` Tenancy admitted to the baseline — one ADR accepted, zero ranked documents amended in content, zero requirements changed, and three pre-existing governance defects repaired.** Issued because a **Rank 3** document entered the baseline (§7 step 4): [`ADR-0050`](./adr/ADR-0050-tenancy-prd-v0.1-baseline.md) admits `30-product/tenancy/PRD-013_TENANCY.md` **v0.1** at Rank 3 as the authoritative specification for **`BC-19` Tenancy** — the **eighth** module baseline, the **ninth** Rank 3 row, and the **first `FOUNDATIONAL`-tier product specification frozen**, so the tenant-partitioning contract every other context depends on is now settled. Four validation records admitted with it, including a **Stage 3 supplement** re-measuring all six checks against the current bytes while the base record's stale hash is **preserved as evidence**. ⚠ **The first v0.x document ever admitted here, and deliberately so:** `ADR-0020` §4 item 2 admits *"each at the version that was verified"* and §5 rejects renumbering at freeze, so the version is **preserved at v0.1** rather than moved to a number no gate measured. `PRD-013`'s only edits are its freeze metadata, its §12 lifecycle table (four rows that were **false** against conferred verdicts, corrected with every superseded statement quoted) and one changelog entry. **No identifier added, removed, renumbered or reworded** — 63 declared, 51 in force, 12 retired. **No `MASTER_PRD.md`, BC Map, Dependency Matrix, `ARCHITECTURE_RULINGS.md`, `TRACEABILITY_MATRIX.md` or `tool/` change. No code.** Four `TEN-GAP-*` admitted **OPEN**; two **code** defects (`D-013-01` `Branch` ownership, `D-013-02` non-async tenant context) carried as `IMPL-801`/`IMPL-813` rather than fixed. ⚠ **`FROZEN` is not `VERIFIED`: 0 of 30 tasks, 0 of 13 acceptance criteria proven by a test.** Rank 2 extends to **`ADR-0050`** (**37 accepted · 13 proposed · 50 files**, re-derived by measurement). Defects repaired and logged: **`GCP-15`** (this header two baselines stale), **`GCP-16`** (§4 behind fifteen ADRs and one freeze), **`GCP-17`** (registry §7 counting frozen `PRD-008` as `DRAFT`) |
| **BASELINE-2026-08-17-A** | 2026-08-17 | **`PRD-008` Revenue & Finance admitted to the baseline — one ADR accepted, zero ranked documents amended, zero requirements changed.** Issued because a **Rank 3** document entered the baseline (§7 rule 4). `ADR-0049` admits `PRD-008_REVENUE-AND-FINANCE.md` **v1.7** (sha256 `d24832f33e600518`, **byte-unchanged** through Stages 3, 4 and 6) at **Rank 3** and confers **Stage 7 Freeze**; `PRD_REGISTRY.md` moves `DRAFT` → **`FROZEN`**. Four validation records are admitted with it. **Obligations 134 → 134, acceptance criteria 98 → 98, gaps 17 → 17** — nothing was written into the PRD to make it pass, and **the count of open gaps did not fall by admitting them**. ⚠ **All 17 `FEE-GAP-*` are admitted OPEN**, and **20 of 83** `IMPL-700`…`IMPL-799` tasks are **BLOCKED** on them, so roughly a quarter of the module cannot be started; `FROZEN` records what `BC-05` **owns**, not what is **decided**. ⛔ **`FINDING A-2` was NOT resolved and no decision was invented for it**: the Architecture Owner authority conferred for this pass was conditional on resolution being *required*, and it is not — `` `ID-1` `` occurs **0** times in `PRD-008`, `ITG-BR-007` **0** times, and `FEE-XC-005` makes holding a gateway credential an **impossibility** for `BC-05`, so the finding is decided *not here* under either reading of Rank 4. It remains **OPEN** with the Architecture Owner, carried in `PRD-019` §7. ⚠ **`ADR-0038` and `ADR-0041` stay `PROPOSED`**; no ADR was promoted, demoted or superseded. ⚠ The **stale `IMPL-227`+ row** in `PRD_LIFECYCLE.md` Stage 6 is **recorded, not edited** — `IMPL-227` is in fact taken by Student Identity, so the range was derived by measurement (`IMPL-700`…`799`, **0** collisions) rather than by trusting the table. **No Dart source was touched**: `git diff --stat 5827068 HEAD -- lib test pubspec.yaml android web` is **empty**, and the 9 pre-existing `ADR-0012` boundary violations are unchanged and unrelated. |
| **BASELINE-2026-08-05-A** | 2026-08-05 | **`PRD-006` Attendance Management admitted to the baseline — two ADRs accepted, one Rank 4 document amended by *removing* what a previous ADR had added, zero requirements changed.** Issued because a **Rank 3** document entered the baseline (§7 step 4): `PRD-006_ATTENDANCE-MANAGEMENT.md` **v1.9**, together with its Stage 3 and Stage 4 validation records, admitted by **`ADR-0034`** — the **seventh** admission ADR, after `ADR-0008`, `ADR-0009`/`ADR-0010`, `ADR-0011`, `ADR-0018`, `ADR-0019` and `ADR-0020`. **`PRD-006` is admitted at `v1.9` and was deliberately not renumbered** — the fourth such precedent after v1.2, v1.4 and v1.0; Stage 7 confers status and does not renumber. Rank 3 now holds **seven** module baselines, and **`BC-03` Attendance is the fourth `[CORE]` cluster context to be frozen**, which closes `PRD_REGISTRY.md`'s own complaint that `PRD-007` was *"a frozen consumer waiting on an unwritten producer"* — the `E-08` occupancy chain is now frozen on **both** sides. **The genuine governance blocker was resolved by withdrawal, not by permission.** Freeze-readiness verification had found that edge **`E-27`** (`BC-03` → `BC-04`), added to the BC Map by `ADR-0032`, sits **outside** the intra-cluster allow-list that `LIBOORA_MODULE_DEPENDENCY_MATRIX.md` L89 states as *"Only the edges `E-01`…`E-10` … Enforced as an explicit allow-list, not 'anything within the cluster'"*, with L202 repeating it machine-readably — and `E-27` was measured to be **the only edge numbered above `E-10` with both endpoints inside that cluster**. **`ADR-0033` was accepted (option `O-C`) and `E-27` is WITHDRAWN**: BC Map → **v1.7**, edge set once more `E-01`…`E-26`, verified at **26 edge rows identical to the pre-`ADR-0032` count** with the neighbouring `E-08` row **byte-identical**. The edge was never architecturally required: frozen Rank 3 `SEAT-FR-104` already requires the Seat Card to compose holder data **from `BC-01` at read time** and **no `BC-01` → `BC-04` edge has ever existed**, so read-time composition across a boundary is an **already-ratified pattern that needs no numbered edge**. **Four things were deliberately NOT done.** (i) **`E-27` was not added to the allow-list** (`O-A`, rejected in writing) — its only effect would have been to make a documentation contradiction disappear. (ii) **`LIBOORA_MODULE_DEPENDENCY_MATRIX.md` was not amended** and stands byte-unchanged at `9895d244494372af`; that is the decisive argument for `O-C` — withdrawing the edge **restores compliance with the law that already existed**, so no Rank 4 law had to be rewritten to let a gate pass. (iii) **`ADR-0032` was not demoted** — its status is unchanged at `Accepted`, was never altered at any point, and its substantive decision `O-5` stands; only its *transport* was withdrawn, and its ⚠ CHALLENGED notices became ✅ RESOLVED. (iv) **No gate was weakened** — `tool/docs_check/prd006_traceability.py` gained a single **filename** in its `ALLOWED` list (**22 insertions: 21 comment, 1 string; zero detection logic, regex or threshold changed**) on an exact two-gate precedent, and when that gate then failed on prose newly written into **this** declaration and into `ADR-INDEX.md`, **the prose was reworded and the gate was left alone**. Admission rests on: **Stage 3 ✅ ALIGNED 6/6**, re-run *after* the dependency decision, with its superseded **⛔ NOT ALIGNED** verdict retained beside the new one; **Stage 4 ✅ verdict A**, 6 of 6; `prd006_traceability.py` **exit 0** (525 identifiers across nine registers, all contiguous, 0 duplicates, 0 dangling, 0 orphan criteria, **0 collisions**, **285/285 = 100.0%**); and `prd006_task_coverage.py` **exit 0** (**80 tasks**, **285/285 = 100.0%**). **All 18 open gap-ledger entries remain OPEN with named owners** and are listed individually in `ADR-0034` §3 — none was closed to make the freeze look cleaner — and **`ADR-0031` remains `Proposed`** because no **Product Owner** has approved a range for the presence tolerance: **a twenty-fifth configuration row was refused rather than invented**, and freezing the PRD did not change that. **Two gates are disclosed RED rather than hidden** (`ADR-0034` §7.4): `prd007_traceability.py` exit 1 (2 problems, proven pre-existing) and `check_module_boundaries.dart` exit 1 (9 `ADR-0012` time-boxed `app → domain/library` findings, red by design). Neither concerns `E-27`; neither gate was modified. **No requirement, event, invariant, acceptance criterion, business rule, register, exclusion, protected operation, configurable or scope statement was added, removed, renumbered or reworded** in `PRD-006`; its only edits are the *Status*, *Baseline*, *Rank* and *Authorities applied* metadata rows plus one changelog entry. **No Dart source was modified** — the diff over `lib test pubspec.yaml android web` against the pre-work baseline is **0 files**. **Three pre-existing drifts in this declaration were corrected in the same commit and logged as `GCP-11`** rather than absorbed silently: §3.1's self-reference read `BASELINE-2026-08-04-D` while the header read `-E`; §3.2 recorded the BC Map at **v1.5** having missed both the v1.6 and v1.7 bumps; and the Rank 2 range still read *"`ADR-0001` … `ADR-0020`"* after `ADR-0032` was accepted. All three are the **same defect class** already logged as `GCP-01` and `GCP-07` — a derived statement left behind by a change to the thing it describes — which is now its **fourth** recorded occurrence and is an argument for a mechanical check rather than for more diligence. **`PRD-006` is `FROZEN`, not `VERIFIED`** — **0 of 80 tasks and 0 of 222 acceptance criteria** are proven, and `SID-4.56` holds that *"a rule that cannot be checked SHALL be treated as unmet"*. |
| **BASELINE-2026-08-04-E** | 2026-08-04 | **`PRD-007` Seat Management admitted to the baseline — one ADR accepted, zero ranked documents amended, zero requirements changed.** Issued because a **Rank 3** document entered the baseline (§7 step 4): `PRD-SEAT-MANAGEMENT.md` **v1.0**, together with its Stage 4–7 gate records and nine review and correction reports as validation records. Rank 2 extended to **`ADR-0020`**. **`ADR-0020`** admits `PRD-007` at Rank 3 as the authoritative specification for **`BC-04` Seating** — the **sixth** Rank 3 module baseline, and the **third** `[CORE]` context of the Library cluster to be frozen, so `BC-01` → `BC-02` → `BC-04` is now frozen along the whole `E-01`/`E-02` chain and `PRD-006`/`PRD-008` inherit settled contracts on every side they touch. **This declaration is as narrow as `-C` and `-D`: no ranked document was amended.** `MASTER_PRD.md`, the Bounded Context Map, the Module Dependency Matrix and `ARCHITECTURE_RULINGS.md` are **untouched**; **no integration edge was added** — `PRD-007` consumes seven existing inbound edges (`E-01`, `E-02`, `E-05`, `E-08`, `E-17`, `E-18`, `E-19`) and publishes on three outbound (`E-20`, `E-23`, capacity alerting), every one already declared in BC Map §7 — no global business rule changed, no dependency law gained an exception, and no bounded context was created (still **31**). **The version is preserved at v1.0** — freeze confers status, not a renumbering, following the `PRD-003`-at-v1.0, `PRD-004`-at-v1.2 and `PRD-005`-at-v1.4 precedents; **no requirement identifier was added, removed, renumbered or reworded on admission** (**683 identifiers** across ten registers, 669 obligation-bearing, 443 criterion-verifiable, **226 `SEAT-AC-*`**, four `SEAT-EVT-*` closed by `SEAT-FR-206`, 14 `SEAT-GAP-*`, all unchanged). Admission followed **Stage 4 verdict A — PASS with zero findings at every severity**, reached after four correction passes, two re-reviews and a final independent review that **rejected eight candidate findings with evidence**; Stage 5 and Stage 6 gates were **re-run at exit 0 immediately before acceptance** (`tool/docs_check/prd007_traceability.py` — 0 dangling, 0 orphans, 0 collisions, `SEAT-FR-302` **85/85**; `tool/docs_check/prd007_task_coverage.py` — **100 tasks `IMPL-500`…`IMPL-599`** in ten waves, non-overlapping with five foreign ranges, **443/443 = 100.0%** of obligations claimed, 0 cycles, 1 root, 1 leaf, critical path 18). **Verification coverage is admitted at its measured value, not rounded up — 100.0% (85/85) of the four registers `SEAT-FR-302` mandates, but 73.4% (325/443) of all obligations**, and the 118-obligation shortfall (86 `SEAT-FR`, 5 `SEAT-NFR`, 18 `SEAT-PO`, 9 `SEAT-CFG`) is named in `ADR-0020` §2.5 rather than hidden. **All fourteen `SEAT-GAP-*` are admitted OPEN and are not ratified by admission** — `SEAT-BR-045` forbids resolving any gap by implementation choice and the Stage 6 backlog assigns them **no task**, listing six tempting tasks that must not be created for exactly that reason; each requires a decision from its named owner. **`SEAT-GAP-009` and the `PRD-006` dependency are disclosed, not closed**: `BC-04` derives occupancy from `BC-03` over `E-08`, and `PRD-006` is still `PLANNED`, so a seating module built today would hold allocations correctly and derive no live occupancy — a scheduling fact, recorded in `ADR-0020` §3.2 so no reader mistakes a frozen specification for a working feature. `TRACEABILITY_MATRIX.md` and `PRD-007_IMPLEMENTATION_TASKS.md` are **deliberately not modified** — Stage 7's gate is a baseline row, and the three stale `IMPL-227+` allocation tables are recorded in `ADR-0020` §3.4 for a post-freeze clarifying pass so the independently verified bytes are preserved. **Two pre-existing omissions in this declaration were also corrected and are recorded rather than quietly absorbed**, as `GCP-07` in §6: §4 never received a Rank 3 row for `PRD-005` when `-D` admitted it, and the Rank 2 row still read *"`ADR-0001` … `ADR-0017`"* after `ADR-0018` and `ADR-0019` were accepted — the same §7 step 3 failure already logged as `GCP-01`, so §4 now lists **six** module baselines. **A third instance of the same defect class was found in `PRD_REGISTRY.md` §4.1 and logged as `GCP-08`** — its prose read *"None was ever written. All nine are `PLANNED`"* above a table showing two PRDs `FROZEN`, stale since `ADR-0018` and unnoticed through two freezes. Neither misconferred a status; both were corrected in this commit, and `GCP-01`/`GCP-07`/`GCP-08` are recorded as one class — a derived statement left behind by a change to the thing it describes — because three instances in three documents argue for a mechanical check rather than more diligence. **One new defect was opened rather than closed**: `GCP-09` records **ten mis-targeted citations** in `PRD-007`, found while authoring the developer guide *after* every gate had passed — `SEAT-BR-018`, `SEAT-BR-020` and `SEAT-BR-022` were renumbered in a Stage 4 pass and three *Authority* pointers in the §8 gate table plus seven elsewhere were left behind. **It is disclosed and left unfixed**, on the Stage 7 rule that a frozen PRD is never silently modified *"not for an obvious correction"*, and it is admitted as a stated limitation of the gates themselves: Stage 5 proves a cited identifier **exists** and Stage 6 proves an obligation is **claimed**, but **no script in this repository compares a citation's meaning to the cited rule's text**. **One Stage 5 gate script was maintained, and the maintenance is recorded rather than performed silently**: `tool/docs_check/prd007_traceability.py` had its collision-scan `ALLOWED` list widened by four **named** files — `ADR-0020`, this declaration, `PRD_REGISTRY.md` and `ADR-INDEX.md` — because recording a freeze requires the governance layer to cite the register it admits, which the list (written at Stage 5) had never had to permit. It follows the existing convention rather than inventing one: `prd005_traceability.py` already enumerates the identical four files for `MM-*` on the reasoning that *"a citation is not a collision"*, and `ADR-0019` cites `MM-*` eighty times legitimately. The list remains enumerated file by file rather than widened to a directory, so a stray `SEAT-` elsewhere in governance still fails the run, and the checks that detect a genuine collision — another register *defining* a `SEAT-` identifier — are unchanged. Both gates were re-run at **exit 0** after the change. `PRD-007` is **`FROZEN`, not `VERIFIED`** — implementation is entirely unstarted, **0 of 100 tasks and 0 of 226 acceptance criteria proven by a test**. |
| **BASELINE-2026-08-04-D** | 2026-08-04 | **`PRD-005` Membership Management admitted to the baseline — one ADR accepted, zero ranked documents amended, zero requirements changed.** Issued because a **Rank 3** document entered the baseline (§7 step 4): `PRD-MEMBERSHIP-MANAGEMENT.md` **v1.4**, together with its nine review and correction reports as validation records. Rank 2 extended to **`ADR-0019`**. **`ADR-0019`** admits `PRD-005` at Rank 3 as the authoritative specification for **`BC-02` Membership** — the **fifth** Rank 3 module baseline, and the second `[CORE]` context of the Library cluster to be frozen, so the `E-01` dependency *"membership may not exist without an active enrollment"* is now frozen on **both** sides. **This declaration is as narrow as `-C`: no ranked document was amended.** `MASTER_PRD.md`, the Bounded Context Map, the Module Dependency Matrix and `ARCHITECTURE_RULINGS.md` are **untouched**; **no integration edge was added** — despite four recorded temptations to add one (`MM-GAP-004`, `MM-GAP-009`, `MM-GAP-010`, `MM-GAP-010a`) — no global business rule changed, no dependency law gained an exception, and no bounded context was created (still **31**). **The version is preserved at v1.4** — freeze confers status, not a renumbering, following the `PRD-003`-at-v1.0 and `PRD-004`-at-v1.2 precedents; **no requirement identifier was added, removed, renumbered or reworded on admission** (333 identifiers across ten registers, 320 obligation-bearing, 223 criterion-verifiable, 97 `MM-AC-*`, seven `MM-EVT-*` closed by `MM-BR-035`, 13 `MM-GAP-*`, all unchanged). Admission followed **five independent reviews and four correction rounds**, the fifth returning **A — PASS with zero findings**; Stage 5 and Stage 6 gates were **re-run at exit 0** immediately before acceptance (`TRACEABILITY_MATRIX.md` v1.5 §2D — 0 collisions; `PRD-005_IMPLEMENTATION_TASKS.md` v1.0 — 42 tasks `IMPL-400`…`IMPL-441`, **223/223 = 100.0%**). **Verification coverage is admitted at its measured value, not rounded up — 95.1% of the rule registers and 60.1% of all obligations**; these are the *corrected* figures that replaced a false 100% the document's own third review retracted, and the four business rules no criterion verifies (`MM-BR-012`, `MM-BR-022`, `MM-BR-027`, `MM-BR-028`) are named in `ADR-0019` §3.5 rather than hidden. **Five gaps are admitted OPEN and are not ratified by admission**: `MM-GAP-001` (`Q-01` grace period), `MM-GAP-002` (`Q-06` proration), `MM-GAP-005`, `MM-GAP-010` and `MM-GAP-010a`, plus `MM-GAP-006a`/`MM-GAP-007a` carried against the BC Map — each requiring a decision from its named owner. **`MM-GAP-010` is admitted as systemic** and supersedes the per-module `SM-GAP-11`/`GCP-06` instance: BC Map §9 names `BC-26` a consumer for eleven producing contexts while §7 declares an inbound edge from only one, so **ten** relationships are asserted in one section of a Rank 4 document and denied by another. `TRACEABILITY_MATRIX.md` and `PRD-005_IMPLEMENTATION_TASKS.md` are **deliberately not modified** — Stage 7's gate is a baseline row, and the one stale statement in each is recorded in `ADR-0019` §3.6 for a post-freeze clarifying pass so the independently verified bytes are preserved. `PRD-005` is **`FROZEN`, not `VERIFIED`** — implementation is entirely unstarted, **0 of 42 tasks and 0 of 97 acceptance criteria proven by a test**. |
| **BASELINE-2026-08-04-C** | 2026-08-04 | **`PRD-004` Student Management admitted to the baseline — one ADR accepted, zero ranked documents amended, zero requirements changed.** Issued because a **Rank 3** document entered the baseline (§7 step 4): `Student_Management_PRD_v1.md` **v1.2**, together with `PRD-004_ALIGNMENT_REPORT.md` v1.1 as its validation record. Rank 2 extended to **`ADR-0018`**. **`ADR-0018`** admits `PRD-004` at Rank 3 as the authoritative specification for **`BC-01` Enrollment**, and delivers Master PRD §8 **module 3** (Library Member Directory) as its **chapter 5** — `LMD-1`…`LMD-31`, an application-layer **read composition** owning no aggregate and no invariant (`AR-1`), so **no `BC-32` was created and the context count remains 31**. **This is the narrowest of the four declarations: no ranked document was amended.** `MASTER_PRD.md`, the Bounded Context Map, the Module Dependency Matrix and `ARCHITECTURE_RULINGS.md` are **untouched**; **no integration edge was added**, no global business rule changed, no dependency law gained an exception, and no bounded context was created. **The version is preserved at v1.2** — freeze confers status, not a renumbering, following the `PRD-003`-at-v1.0 precedent; **no requirement identifier was added, removed, renumbered or reworded on admission** (251 base / 266 total identifiers, ten registers, ten `SM-EV-*` events closed by `SM-7.12`, 32 acceptance criteria, all unchanged). Admission followed **three independent reviews and two correction rounds**, the last an adversarial re-audit that attempted ten disproofs and failed on eight outright; six findings were **rejected with evidence** and the draft's intent preserved. **Two gaps are admitted OPEN and are not ratified by admission**: `SM-GAP-10` (`GCP-05`, DOB Optional vs `SID-5.9` — internal to `PRD-003`) and `SM-GAP-11` (`GCP-06`, BC Map §8 vs §7 over `BC-26`), each requiring a **separate ADR from its named owner**. `PRD-004` is **`FROZEN`, not `VERIFIED`** — implementation is entirely unstarted, 0 of 24 tasks and **0 of 32 acceptance criteria proven by a test**. |
| **BASELINE-2026-08-04-B** | 2026-08-04 | **Governance closure — five ADRs accepted, one Rank-3 and one Rank-4 document amended, zero requirements changed.** Issued because a **Rank 3** document changed version (§7 step 4): `Library_PRD_v1.md` v1.0 → **v1.1**. Rank 2 extended to `ADR-0017`. **`ADR-0013`** (capability contexts are platform-owned) amends the Library PRD's *Owning contexts* header row to separate the one context it owns (`BC-06`) from the three it consumes through ports (`BC-19`, `BC-25`, `BC-29`); the aggregates `TenantOrganisation`, `StaffAssignment` and `LibraryBranding` values remain owned there — **this is not a scope reduction.** **`ADR-0014`** (tenant-key and audit-mutation enforcement) raises the Dependency Matrix to **v1.3** and its enforcement coverage from *10 of 12* to **12 of 12** categories, making `X-10` and `X-13` mechanically checked and therefore *met* under `SID-4.56`. **`ADR-0015`** corrects three wrong finance context identifiers in the Library PRD (`BC-11`…`BC-13` → `BC-05` Fee & Collection and `BC-20` Subscription & Billing); the ownership boundary those rows assert was already correct, only the identifiers were wrong. **`ADR-0016`** adds `BC-10` to edge `E-22`'s consumer list in the BC Map (**v1.4**), an edge `SID-4.35` already depends on and which BC Map line 292 requires an ADR to add. **`ADR-0017`** resolves `BC-25` Configuration ownership: it is a named **V1 product module** (Master PRD §8 module 18) and receives a dedicated **`PRD-023`**, registered on the precedent `PRD_REGISTRY.md` §4.3 set for `PRD-022` — not invented to fill a gap. `PGA-01`, `PGA-02`, `PGA-06` and `PGA-11` closed; contested contexts **3 → 0**. **No requirement identifier was added, removed, renumbered or reworded in any PRD**, and no dependency law gained an exception. **Two pre-existing staleness defects in this declaration were also corrected**, and are recorded rather than quietly overwritten: §3.2 stated the Bounded Context Map at **v1.3** when the file was already at **v1.4**, and the Module Dependency Matrix at **v1.1** when the file was already at **v1.2**. Both drifted because v1.4 (BC Map, implementation-status row) and v1.2 (Matrix, fitness-function-executed row) were self-recorded in those documents without the §7 step 3 update to this declaration. Logged as **`GCP-01`** in §6. |
| **BASELINE-2026-08-04** | 2026-08-04 | **`BC-10` Global Person Identity admitted to the baseline, and the identity architecture finalised.** `Student_Identity_PRD_v1.md` v1.0 (Chapters 1–5) and `STUDENT_IDENTITY_ALIGNMENT.md` declared authoritative at Rank 3 — the **third** module baseline, alongside Authentication (`BC-18`) and Library Management. `ADR-0011` accepted — Rank 2 extended to `ADR-0011`. **`ADR-0011` amends the Bounded Context Map §4 Identity Triad**: `BC-10` renamed Global Person Identity, reclassified `[SUPPORTING]` → `[CORE]`, moved out of the Social cluster to **rank 7.5**, cardinality `0..1` opt-in → **`1:1` mandatory**. Linkage rules `ID-1`…`ID-6` and prohibition `X-05` preserved unamended; `ID-4`'s `PersonId` becomes non-nullable. BC Map raised to v1.3; Module Dependency Matrix to v1.1; Master PRD to v1.7. `tool/module_dependencies.yaml` gains `domain/person: 7.5`. Nine registers declared: `SXC-1`…`11`, `SID-BR-1`…`18`, `SPO-1`…`9`, `SEV-1`…`16`, `SID-INT-1`…`12`, `SID-INV-1`…`14`, `SCFG-1`…`11`, `SID-AC-1`…`26`. `Q-05` closed. **One global business rule was changed — `MP-GBR-02`** — the first such change since the baseline was established; it is recorded in Master PRD §36. Two review findings were rejected and the supplied draft's intent preserved. No requirement was withdrawn and no earlier decision reversed. |
| **BASELINE-2026-08-03** | 2026-08-03 | **Library Management admitted to the baseline.** `Library_PRD_v1.md` v1.0 (§§1–25), `14B-Public-Library-Preview.md` v1.0 and `INVITATION_SECURITY_SPECIFICATION.md` v1.0 declared authoritative at Rank 3 alongside §14A. `U-4` closed by receipt; `AR-4` invitation-security deferral closed by authorship; `CC-5`, `CC-6`, `CC-7` closed. `ADR-0009` and `ADR-0010` accepted — Rank 2 extended to `ADR-0010`. Architecture Rulings raised to v1.2; Master PRD to v1.6; Traceability Matrix to v1.1. Thirteen Library configurables `LCFG-1`…`LCFG-13`, ten invitation configurables `ICFG-1`…`ICFG-10` and seven invariants `INV-10`…`INV-16` added to the Configuration Guide. No requirement was withdrawn and no earlier decision reversed. |
| **BASELINE-2026-08-02** | 2026-08-02 | Initial declaration. Authentication PRD v2.0 declared official; `D-7` closed by authorship; EA raised to v2.1; `CFG-3`, `CFG-4`, `CFG-5`, `CFG-6`, `CFG-7`, `CFG-12` reset to standards-anchored defaults; `ADR-0001`…`ADR-0008` accepted; twelve stale documents corrected or archived. |
